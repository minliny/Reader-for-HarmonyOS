# Reader-for-HarmonyOS 工作规则

## 验证问题先记录、先从代码侧定位

遵守用户于 2026-09-11 更新的[工作区操作原则](../AGENTS.md)：所有验证中发现的问题必须记录，并优先使用当前源码、已有证据及本地探针审计定位。只有代码侧无法定位剩余问题，并已记录审计范围、未决点及最小取证目的后，才允许真机测试、验证或抓取。代码侧已经能够定位的问题先修复并做本地回归，不能用重复真机取证替代；此前已释放真机不会因此自动重新获得使用授权。

## VM 与 HAP 交付硬规则

以下规则来自 2026-09-05 的 VM 启动事故，适用于所有构建、检查、安装、启动和回归任务。

1. 用户说“安装到 VM”时，只能使用已经存在且已确认身份的目标 VM。该授权不包含新建、克隆、
   重置、擦除、删除或替换 VM，也不包含改变其他 VM 的状态。任何此类操作都必须取得用户针对该
   动作的明确授权；不得为了绕过连接或启动故障创建“临时 VM”。
2. 操作前必须以当前现场重新确认 VM 名称、实例路径、HDC serial/endpoint 和运行状态；不得复用
   历史端口、历史实例名或仅凭目录名猜测目标。共享 VM 必须按项目既有方式加锁。
3. `HDC Connected` 只表示传输通道存在，不表示 HarmonyOS 已完成启动。执行 `bm dump`、安装 HAP、
   启动应用、导入数据或交互验收前，必须同时满足：
   - 命令明确绑定到已确认的唯一 HDC target；
   - `bootevent.boot.completed=true`；
   - Emulator 日志已经出现 `Guest OS Boot Completed!!`；
   - SceneBoard 已稳定运行，未处于连续 `LIFECYCLE_TIMEOUT`/退出循环。
4. 在系统尚未就绪时，只允许做最小、只读的启动状态检查。禁止因为等待时间长而反复冷启动、
   停止/启动、并行探测、查询包信息或尝试安装。需要冷启动、重启或停止现有 VM 时，先说明原因和
   数据风险；任务没有明确授权时不得执行。
5. 一旦出现 `SceneBoard exits 4times in 240000ms`、`LIFECYCLE_TIMEOUT`、guest reset、kernel panic
   或 HDC 反复掉线，立即停止所有应用层操作和自动重试，保留 Emulator、kernel、hilog/faultlogger
   原始证据。不得用新 VM、清数据、删快照或重新安装来掩盖故障。
6. VM 启动故障必须按时间轴关联 Host/Emulator、guest boot、SceneBoard、HDC、Bundle Manager 和
   Reader 进程证据。只有看到 Reader 的启动或崩溃记录，才能把故障归因给 Reader；“VM 里只有一个
   用户应用”不能替代系统服务证据，也不能据此跳过 SceneBoard 等系统组件的排查。
7. 安装默认保留应用数据。未经明确授权，禁止卸载、清数据、恢复出厂、替换 userdata、删除 VM
   或执行任何等价操作。书源、书架、阅读进度和用户配置都按不可丢失数据处理。
8. HAP 构建、签名、VM 安装、应用启动和功能交互是五个独立证据层。安装只有在 manifest 绑定的
   HAP 成功写入已确认目标并生成有效部署回执后才可标记通过；`Device not found or connected` 等失败
   必须保持 `vmInstall: OPEN`，不得声称已安装，也不得改装到另一个 VM 作为替代结果。
9. 故障报告必须区分“直接触发”“底层阻塞”“agent 的操作贡献”和“尚未证明的推测”。不得把
   `Connect server failed`、白屏、慢启动或系统重启笼统描述成 VM 损坏，也不得在没有进程/日志证据时
   归因给应用、用户或外部操作者。
10. 所有正式构建、签名、复验和保数据部署继续以 [`../HAP_BUILD_SYSTEM.md`](../HAP_BUILD_SYSTEM.md)
    及项目 HAP pipeline 为唯一入口；本文规则是其 VM 安全前置条件，不能被脚本成功、测试通过或
    交付时限绕过。

## 事故经验

2026-09-05 的旧 VM 并未损坏：镜像校验成功，最终也完成了启动。两次失败的直接触发是 SceneBoard
启动阶段阻塞于文件访问/临时数据清理，连续四次生命周期超时后由系统保护机制主动重启。agent 的
错误是反复启动实例，并在只看到 HDC 连接、尚未确认 guest boot 完成时继续执行包查询和安装尝试；
随后又未经授权创建了新 VM。新版 Reader HAP 当时没有安装成功，也没有 Reader 启动证据。后续遇到
相同现象时必须等待完整就绪门禁或停止并报告，不能重演上述操作链。
