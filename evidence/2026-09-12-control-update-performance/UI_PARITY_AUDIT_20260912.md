# 控制栏新增问题：Figma / Make / 当前代码对照

日期：2026-09-12  
范围：快捷设置、快捷朗读、完整朗读、会话播放胶囊、沉浸阅读顶栏、完整控制栏收起。  
对照源：当前 `entry/src/main/ets` 工作树、已有 Figma 派生几何记录、此前保存的 Figma Make 源码与已有 VM 证据。

本轮只做审计和证据记录，没有修改生产样式、动效或布局代码。Make 的设置内容页在已保存的入口源码中不存在，因此设置页只能与 Figma 派生几何、当前 Reader 组件规范和现有 HarmonyOS 设置组件对照，不能声称存在一个缺失的 Make 画面作为依据。

| 用户现象 | Figma / Make 对照 | 当前代码定位 | 结论 | 需要决定的事项 |
|---|---|---|---|---|
| 快捷控制栏设置的页面外框丢失 | Figma 派生设置容器应有独立外框；已有 Make 朗读卡片使用暖色渐变和 0.5 线。保存的 Make 入口没有设置内容页。 | `ReaderControlSettingsContent.ets:211-222` 的父 Stack 只有背景和圆角，没有 border。 | **代码已定位，视觉缺口明确。** | 是否按 Figma 派生的设置容器边线和现有 Make 卡片边线规则补齐；具体颜色/线宽以 Figma 变量为准。 |
| 设置子标题被选项覆盖 | Figma 派生几何中标签和选项条是两条独立轨道。 | `ReaderControlSettingsContent.ets:205-222` 将标签与选项作为同级绝对定位 actor，且共享 `clipShape`；中间进度下没有单一父级排版约束。 | **疑似动态裁切/轨道重叠，静态端点不能证明已复现。** 缺口与外框缺失可能叠加。 | 是否接受“标签始终保留独立安全间距”的 Figma 解释；若确认，需在代码中给 label/option 设统一的中间态约束。 |
| 快捷设置标题错位并显示不全 | Figma Settings master 完整端点将标题放在 y=9、第一项标签 y=37、选项条 y=61；快捷端点不应复用同一组相交坐标。 | `ReaderControlSettingsGeometry.ts:74-80` 快捷标题 y=17..33，第一项标签 y=15..27、选项条 y=32..62；`ReaderControlSettingsContent.ets:185-224` 对轨道分别绝对定位和裁切。 | **代码已定位，属于本次漏列的独立问题。** 当前未修复。 | 需按 Figma 轨道重排快捷端点，并验证 p=0、0.5、1 的遮挡/裁切；不能只移动 Text 节点而留下选项条覆盖。 |
| 设置选项外框和圆角丢失 | Figma 派生设置选项应保留选中/未选中容器形状；Make 同类预设按钮使用圆角和未选中边线。 | 选项节点只在 `ReaderControlSettingsContent.ets:213-216` 写了 `borderRadius(6)`；`ReaderControlSettingsOptionModifier.ets:30-36` 的动态属性没有 border，未选中项也没有边线。 | **代码已定位。** 圆角声明存在，但动态 modifier 未提供完整形状属性，未选中边线明确缺失。 | 是否按 Make 预设按钮的 0.5 线、6 圆角统一到设置选项；需要确认 Figma 是否要求选中项也显示线。 |
| 朗读快捷页“播放”下方文字丢失 | Make 快捷播放标签包含标题和状态副标题；Make 参考宽度下状态文本完整可见。 | `ReaderControlTtsContent.ets:414-425` 确实渲染副标题，但 `ReaderControlPlaybackGeometry.ts:79-81` 给快捷标签固定约 106vp 宽；图标占 34vp、间距 9vp 后，状态 Text 只能拿到很小的剩余宽度并触发 ellipsis。已有 VM 探针测得文字可用宽约 29.71vp。 | **代码已定位，属于宽度分配问题，不是文案未渲染。** | 选择：缩短状态文案、扩大快捷卡/标签宽度、或只在极窄状态省略；建议优先保持 Make 两行结构并给副标题设最小可读宽。 |
| 完整朗读语速左侧需要新增 0.50x | Make 源码已有滑条最小值 `0.5`，但预设按钮只有 0.75/1.0/1.25/1.5。 | `ReaderControlTtsContent.ets:669` 预设只有四项；`ReaderControlTtsContent.ets:695` 的滑条最小端标签已由共享常量提供。 | **这是明确的新产品要求，不是旧 Make 的遗漏。** | 是否新增为第五个预设按钮；需同时确认 280/312vp 视口下五按钮的换行或横向滚动规则。 |
| 自动翻页/朗读启动时，胶囊展开后才闪现图标文字 | Figma 动效约束是一个共享可逆进度、各 actor 独立轨道；已有 Make 只提供静态胶囊，没有原生展开时序。 | `ReaderSessionMorphTimeline.ts:15-23` 把 reveal 起点设为 expand 完成后；`LocalReadingExperience.ets:7900-7901` 在 expand 阶段把 shell 展开，但 content 仍保持完全负向偏移。 | **代码已定位，原因是内容 reveal 串行晚于 shell expand。** | 是否改为 shell 展开和内容 reveal 有重叠的独立 actor 轨道；这会改变动效节奏，但仍可保持同一共享时钟。 |
| 沉浸阅读顶栏应与系统状态栏平齐；打开控制栏后状态栏正常显示，控制顶栏不能重叠 | Make 顶栏是应用画布 y=19 的静态参考，不定义系统状态栏策略。Figma 派生阅读 chrome 使用 25vp 基准和 live safe-top + 8vp。 | `ReaderPageChromeLayout.ts:5-7,103-112` 使用 25vp 与 safe-top+8；`LocalReadingExperience.ets:7327-7334` 直接沿用 `snapshot.hideStatusBar`，默认回退值在 `:7337-7345` 为 true。打开控制栏没有强制显示状态栏的分支。 | **位置规则与窗口策略分别已定位。** “打开控制栏必显状态栏”与现有用户隐藏状态栏偏好存在产品取舍。 | 请选择：A. 控制栏打开时临时显示状态栏并按 live inset 下移顶栏；B. 始终尊重用户隐藏状态栏设置，只修正不重叠的 safe-top。 |
| 完整控制栏“收起”不生效 | Make 仅切换演示分支；原生必须保持真实状态。 | `ReaderControlPanel.ets:862-872` 的按钮已有独立命中层和 `collapseControl()`；此前修复了 shell 截获点击的问题。VM 非 PTY 证据显示点击后约 270ms 隐藏，约 640ms 展开稳定。 | **通用共享控制栏路径已在代码和 VM 定点通过；尚未证明所有模块/旧组件路径都通过。** | 若仍能看到失败，需要提供具体模块/状态，以确认是否误点到旧 FullPanel 或 overlay；不建议在没有新证据前重复改样式。 |

## 证据边界

- 代码定位、Make/Figma 对照和现有 VM 定点证据已经分开记录；现有 VM 端点不能替代逐帧视觉验收。
- 本轮没有真机抓取，也没有重新构建 HAP，因为当前问题均已能在代码侧定位，或已经明确是设计取舍；按工作区验证原则不以真机重复确认代替代码审计。
- 快捷设置没有对应的已保存 Make 内容页，这是来源缺口，不能把当前设置画面直接宣称为“偏离 Make”。

## 待用户拍板后再执行

1. 设置容器/选项的边线、圆角和中间态间距，按 Figma 派生值落地。
2. 快捷播放副标题的最小可读宽度与窄屏省略规则。
3. 是否加入完整语速的 0.50x 预设，以及五按钮的响应式布局。
4. 胶囊内容是否与展开轨道重叠 reveal。
5. 控制栏打开时是否强制显示系统状态栏（A/B 两种策略见上）。
