# VM修复验证记录

当前已安装 `20260911T091131Z-35f2f99a-557f7225`，保留原VM和数据，未用真机。完整52项与当前修复、失败、性能边界统一见[全量总账](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/CURRENT_ISSUES_AND_REPAIR_PLAN.md)及[机器记录](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/execution-status.json)。

b901长按+音量键、暖/冷滚动切分页往返通过；642图片真实卷曲通过。b901四模式40次双向快速点击复测进行中。单调时钟修复与离屏树复用已装包，尚未全量验收。


557新增验证：两页前进/返回的跨章文本、图片中间卷曲像素通过；离屏复用性能与四模式40+40重测进行中。b901连续仿真轨迹仍保留81.562ms长任务（guest调度65.889ms），不因旧短样本9.485ms而忽略。

<!-- PRE_REPAIR_VM_AUDIT -->

原修复前VM审计（以下“不改源码／未安装”等仅描述原采样时点）：

# VM 运行取证与修复方案更新

2026-09-10，采样约 18:42–18:57（北京时间）。本轮只做现有 VM 的正常 UI 操作、公开系统 trace 和本地分析；未修改业务源码、未构建或安装 HAP、未使用真机。52 项原总账全部保留，本文提升其中部分证据等级，不代表全部问题已关闭。

**结论：已有覆盖全部已知问题的实施方案；运行审计进一步确认了控制栏长任务、双向翻页重挂载、热页起手成本和收尾附近的 GC。仍按住迟起动的旧请求因果链、真实送显延迟及全模式视觉矩阵仍未完成运行闭环。**

## 1. 包身份与测量口径

- 现有 Mate 80 Pro VM，1280×2832，HDC `127.0.0.1:5555`。获取共享锁前复核 boot completed、GuestOSBootCompleted、稳定 SceneBoard 与系统日志；没有新建、重启、清理 VM。
- 测试绑定 manifest：`20260910T013945Z-35f2f99a-9821e2a2`；签名 HAP SHA-256 `31cae9eff9914325edeadf474ae9e28365f778b97a6ee914869ff7cd044df8ad`。已有部署回执与本轮 `package-inspect.log` PASS 相互核对。本轮没有安装包。
- 结束时重新核对 9 个关键 Host/Native/阅读/控制源码文件，均与上述 manifest 的 source snapshot 一致，详见 `final-source-identity.json`。这是所列文件与被测包的关系，不代表共享工作区全部文件未变化，也不代表释放 VM 后仍安装此包。
- 使用系统公开 `hitrace`，含 ArkUI、输入、动画、图形、Ark 与 guest 调度事件。4 段控制栏有效记录、4 段翻页记录均保留原始 ftrace；App 同步 slice 的起止配对没有未匹配项。
- 本段 VSync 周期约 **15.626ms**。下文的“帧任务”指 App 主线程 `OnVsyncEvent` 的墙钟耗时；它不是屏幕实际展示时长或物理触控延迟。guestScheduledMs 是 VM 内核认定该线程在运行的时长，仍不能等同宿主物理 CPU 占用。
- 性能录制窗口内没有截图、布局 dump；端点检查在录制完成后进行。工具、trace 和 VM 自身仍有开销；这些是问题定位样本，不能从 4 次手势估算可靠 p95/p99。

## 2. 控制栏：实际长任务已确认

| 操作 | 最大 UI 帧任务 | 主要证据与位置 |
|---|---:|---|
| 隐藏→快捷，首次录制 | 75.263ms | UP 后约 15.8ms 开始；ReaderControlPanel 更新 51.399ms，其中 guest scheduled 37.845ms；整帧 guest scheduled 55.809ms |
| 目录快捷→完整，第 1 次 | 16.613ms | 主要峰值在 UP 后约 321ms，靠近尾段 |
| 目录快捷→完整，第 2 次 | 26.886ms | UP 后约 330ms；另有 UP 后约 33ms 的 19.535ms 任务 |
| 目录完整→快捷 | 25.346ms | UP 后约 41ms；约 84/107/131ms 另有 22.723/23.360/17.569ms 任务，约 260ms 再有 20.793ms |

收起过程出现连续超预算任务，展开重复录制的尾部也有长任务。现在可以排除“只有主观慢尾、没有实际 UI 开销”这一解释。但本轮没有记录生产进度 p 对应的每次显示帧，不能把用户所见的某一停留精确归到某个 p，不能把所有停顿都归给一个函数。

当前生产 `ReaderControlPanel.frame()` 仍每次完整采样并创建几何，build 多处调用；内容宽高、目录列表及滤镜成本仍需拆分。系统 trace 将问题缩小到实际生产 Panel 更新/布局，尚不能单靠其名称分解为 frame()、Path、scrollTo 各自几毫秒。

**修复顺序：** 同一次状态更新只计算一份最终几何；按尺寸/内容版本缓存端点和 Path；只移动的内容保持布局；目录用稳定行 key 和语义锚点合并必要修正；降低动画中无关准备和分配。消除长任务后，再核对清晰度平台和慢尾。保留 Figma 各元素轨迹，不能整体缩放文字或替换为简单淡入淡出。

## 3. 双向翻页：真实重挂载已确认，热页仍有起手成本

实际 UI 原本已选“滑动”（即本任务所说平移），未改变设置。对同一章节第 1/12 页与第 2/12 页，执行两轮 next→previous 镜像轨迹：500px/500ms 移动，终点停住约 500ms，随后 UP。

| 手势 | DOWN 至 UP 内最大 UI 帧任务 | 同一任务 guest scheduled | 本轮联系 |
|---|---:|---:|---|
| next 第 1 次 | 175.023ms | 107.271ms | 起手阶段；布局 115.037ms，Stage 更新 33.470ms |
| previous 第 1 次 | 46.555ms | 44.227ms | 起手阶段重新构建正文 |
| next 第 2 次 | 36.770ms | 36.185ms | 重复同页对仍重新构建正文 |
| previous 第 2 次 | 21.000ms | 19.510ms | 重复同页对仍重新构建正文 |

四次全部出现相同结构：起手时 **Destroy 一份 ReadingSurface，再 Build 两份 ReadingSurface**；UP 后收尾时又 **Destroy 两份、Build 一份**。第一次 next 的起手记录在 DOWN 后约 84ms 销毁旧正文，约 137ms 和 206ms 构建两份正文。重复 next 仍在约 44/54/65ms 出现同类销毁/构建。

这使 F05/P06 从“当前条件分支可能重挂载”升级为 **被测包中实际发生重挂载**。固定 `.id()` 没有跨条件分支保住实例。它不是首次进入应用才发生的成本，四次动作的起手和收尾都发生了。不能由此声称每个 MOVE 都销毁重建；持续段与边界段应分开统计。

同一热页对的本轮样本没有支持“previous 总比 next 慢”：两个方向都有问题，首轮 next 反而更慢。用户描述的方向差异继续保留，P01/P10 的准备优先级和上一页索引成本不能由这一小样本否定。

**修复顺序：** 把两个物理页槽移到稳定同级结构中常驻，变更角色/位置/绑定，不在 idle、方向、提交之间替换整棵正文树；冻结一次动作的页面投影和布局；高频偏移只驱动位移与必要阴影。把字体/主题/图片/高亮/视口等失效纳入版本管理；空闲重绑远端页时不得清空仍参与显示的槽。回归要直接验证生命周期计数，普通动作起手和提交不得再产生这些正文 Destroy/Build。

## 4. 停住仍按着：输入场景有效，延迟起动因果仍开放

系统记录包含 DOWN、移动到终点、终点坐标保持、UP。四次从首次终点坐标到 UP 分别约 487/486/486/481ms，确认不是“已经抬手”的序列。系统工具在保持期间仍发送同坐标 MOVE；因此它覆盖静止坐标且触点未释放，但尚未覆盖完全没有后续 MOVE 的静默 Hold。

本轮没有源头标记把每次 owner/事务启动关联到输入序号、旧 rapid、准备结果或 Auto Page。也没有逐次位移的显示关联。因此不能宣称复现或排除用户原述的“停住才起动”，更不能把 175ms 帧任务当作该旧队列漏洞的运行证明。

P13 的 next/previous 生产决策链本地复现仍成立，优先级不变：DOWN 即取得指针所有权；旧 rapid、准备/纹理回调、Auto Page、音量键统一仲裁；UP 最后样本与所有权释放原子完成。下一轮带有界诊断的测试需要在静默 Hold 内精确释放旧准备结果，并记录来源、seq、epoch、anchor、事务、实际位移提交；未取得此证据前 P11/P13 运行闭环保持 OPEN。

## 5. GC 和页面恢复：补充发现与边界

- next 第 2 次在 UP 后约 120ms 有 App `PartialGC::RunPhases` 21.264ms，位于收尾时段附近。GC 是已观察到的额外开销，不能把它全算进某个几何函数。
- previous 第 2 次完整 6 秒记录的最大帧任务是 102.656ms，但发生于 UP 后约 707ms，**不在本轮按住拖动段**。其中 ReaderShell 更新 87.437ms，嵌套 GC 76.538ms、GC::WaitFinished 60.006ms；Shell 更新的 guest scheduled 仅 24.165ms。不能把整段 87ms 都说成 Shell 自身计算，也不能把 102ms 当上一页拖动峰值。
- 后台/收尾分配及 GC 需要进入 P12/N11–N13 的统一预算，在连续下一次手势开始前可与其重叠。此次没有证明泄漏、GC 的全部分配来源或宿主调度根因。
- 两轮 1→2→1 后，正文文字、坐标、页码与百分比一致；差异仅时钟。`roundtrip-body-comparison.json` 保留原始比较和排除时钟的结果。此前 P08 的“第 6/11 页、44% 同页码不同首行”没有在相同锚点条件复现，本轮不关闭它。

## 6. 更新后的完整实施顺序

1. **统一指针与事务正确性：** P02–P05、P07、P13、P15，R01/R02。处理最终 UP、取消、边界、旧队列、unknown 提交和退出保存；以生产状态序列测试验证。
2. **双槽常驻与高频隔离：** F05/P06/P12/P14。此次已获得运行优先级依据；完成后以相同 4 条轨迹重放，正文生命周期零重建作为结构性出口，再看起手任务和跟随延迟。
3. **双向准备和索引：** P01/P10/N07。隔离可见上下文，复用版本化前驱索引，按请求方向提升优先级，主线程测量/截图分预算，冷触点续接不补播旧轨迹。
4. **生产控制栏：** C01–C07。同帧几何、稳定内容布局、列表修正、分配和后台工作预算；重复展开/收起后无连续超预算任务。C08 仍为最近书签语义核对项。
5. **仿真及呈现：** N01–N10/N14–N15、F01–F04/F06–F09。消除 24ms 主动延后和起手追赶，原子结束样本，同代次不重隐藏，事务固定纹理，提交前连续再抓，保留不透明底和正确版本交接；submitted 与 presented 分开。
6. **资源、滚动和独立遗漏：** N11–N13/P08/P09/R03–R05。GC/上传/复制/空闲调度，图片锚点补偿、多指归属、上一章定位、无动画等待和同页不同正文调查。
7. **统一交付和验收：** 所有步骤都绑定同一候选源/包，按项目 HAP 流水线保数据部署到既有 VM。持续 100 次、再抓、失败注入、全模式/主题/生命周期矩阵按原总账执行；未测项继续 OPEN。

完整逐项修法与验收矩阵见 [52 项总账](../2026-09-10-page-turn-physical-b1f20b88963d/CURRENT_ISSUES_AND_REPAIR_PLAN.md) 和 [完整执行方案](../2026-09-10-page-turn-physical-b1f20b88963d/EXPANDED_AUDIT_AND_EXECUTION_PLAN.md)。本轮没有删除旧条目，也没有以少量 VM 样本替代原验收。

## 7. 仍不能关闭的验证

本轮只填补控制栏部分生产路径长任务、滑动两向同章重挂载/起手、静止坐标 Hold 输入以及一个页对往返的证据。下列仍开放：

- 控制栏七模块全程、拖动/停住/反向/再抓、深列表与异步更新，进度→显示帧及 Figma 对照。
- 旧请求在静默 Hold 中返回的运行因果；Auto Page/音量键独立来源，跨章/冷恢复、方向差异分布、稀疏输入、最后 UP、多指和 CANCEL。
- 仿真 Native 端输入/纹理/求解/上传/提交/实际显示；覆盖、无动画、滚动模式独立回归；日夜视觉、动态高亮、成功/回滚/失败/快速交接。
- P08 原条件的权威 scalar/布局版本/提交锚点；保存未知、晚回执、退出与恢复等失败注入。
- p95/p99、连续 100 次、120Hz、再抓跳变量、内存峰值/长跑/空闲；真实物理触控、GPU/显示、温升和电池。本轮不占用真机，也不把 VM 数字映射为真机结论。

## 8. 释放与复现材料

测试结束恢复至诡秘之主第一章第 1/12 页、0%，控制栏关闭，“滑动”设置保持原样。18:59:08 probe 正常 quit；18:59:09 本任务验证目标锁已不存在。没有删除锁、抢占其他任务或在释放后继续操作 VM。记录见 `VM_RELEASE.json`；此后设备状态可以由其他持锁任务改变。

原始 trace、端点截图/布局、采样参数、SHA、分析结果均在本目录。`analyze_trace.py` 输出 `trace-analysis.json`，单独列出触点接触期间峰值、正文生命周期与 GC，避免把整个录制窗口的最大值混作手势耗时。一次点击落在录制窗口外的收起尝试被排除，随后重新同步采样得到有效记录。
