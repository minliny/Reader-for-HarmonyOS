# 人工审视追加反馈 PH48–PH55

用户于2026-09-14追加8项问题。初始审计源码Harmony9a8ef9a1（clean）；最新已安装回执仍为643bcaf5，PH43–47产物14e814fa已构建但未安装。不得混淆用户观察的已安装版本与当前源码。先记录、代码定位和本地回归，本轮不操作设备。

|ID|现象及要求|初始状态|
|---|---|---|
|PH48|快捷/完整控制栏的小横条相对高度偏移，视觉位置错误。|OPEN；比对同一抓手轨迹、面板顶缘与命中区。|
|PH49|快捷小横条可上拉展开，完整小横条不能下拉收起。|OPEN；审计触摸命中、手势归属、反向状态提交。|
|PH50|快捷搜索输入底边被拉伸居中，搜索按钮缺少外轮廓。|OPEN；比对当前Builder、几何及原稿。|
|PH51|自动翻页快捷控制栏顶部栏位缺失。|OPEN；审计模块标题和内容顶部控件的显示/裁切。|
|PH52|替换完整控制栏部分文字缺深色适配；新增规则缺底部保存/取消，顶部取消含义不清。|OPEN；审计主题消费者和编辑操作完整性。|
|PH53|新增规则输入圆角/字号过大，输入内重复标题备注。|OPEN；按当前设计证据和用户明确修正调整。|
|PH54|新增规则默认以当前书籍/当前书源为范围。|OPEN；审计创建入口和真实身份字段，保护已有规则。|
|PH55|快捷章节进度条用于连续调节，章节改变后不应收起控制栏。|OPEN；审计提交章节回调与控制栏关闭耦合。|

后续逐项补充原因、实现、参考来源、本地证据及仍需交付后验证的边界。PH42多级目录、原完整计划中的未完成项继续保留，本记录不替代历史总账。

## 当前实施与证据

|ID|代码侧处理|剩余边界|
|---|---|---|
|PH48|绘制直接消费唯一grabber位置/尺寸，保留原稿顶距9vp、42×4及72×28命中框，消除Stack margin和actor两处位置来源。|已装643的实际像素高度差尚无对应证据，不能宣称已证明具体设备根因。|
|PH49|面板拖到隐藏端点时，持有手指的父树仍接收触摸，直到Up/Cancel才失活。沿用已定Full下拉关闭整体、点击横条回Quick。|真实原生事件派发与设备观感待新包验证。|
|PH50|底线贴近真实搜索行、修首条结果裁切，补输入/按钮描边；禁用只淡化图标。|设备像素待验；Full几何/结果屏幕轨迹、字号保留。|
|PH51|原24vp顶部槽补模块名及真实状态，正常内容/胶囊源投影一致，沿用无返回要求和同一p。|新包实际动画视觉待验，未称旧图为新版本验收。|
|PH52|输入显式深色前景；底部固定保存/取消，顶部返回只退出临时编辑；确认失败保留草稿。|原生键盘与长表单可见区待验。|
|PH53|输入明确11fp、6vp圆角、描边；标题保留备注，输入placeholder清空。|尺寸依据现有紧凑控件及用户修正，不冒称来自Figma新增表单。|
|PH54|传真实书名/sourceId，仅新建默认scope，编辑已有/独立设置管理入口保持原值。|沿用Core书名或书源任一命中；local不加入全部本地范围，不伪称复合ID精确约束。|
|PH55|章节进度条跳转成功不收起；连续End/Click、延迟选章与失败重试保留此意图。|真实设备连续交互待验，目录/搜索结果原关闭策略保持。|

搜索/自动顶部见[PH50/51](PH50-51-QUICK-HEADER.md)，规则表单见[PH52–54](PH52-54-replace-editor-audit.md)。定向SDK属性/业务方法回归已通过；完整构建及签名由全部生产和测试冻结后的统一pipeline负责。未操作真机/VM，原已装643bcaf5和历史OPEN不变。

PH49回归中又发现同一路径的反向边界：Quick下拉隐藏、向上回拉、再次下拉会过早进入展开段，生成退化的隐藏→隐藏段，导致松手回Quick。已在现有Session分段条件中要求先回到捕获的可见度再跨段，不另写手势引擎。7模块×Quick/Full及28条跨18vp后停住2秒仍按着→Up/Cancel均通过，Full下拉/Cancel/点击含义与既定规则一致。Panel→LRE→ReaderShell祖先审计没有第二处visibility=0门禁。

## 首次全量门禁失败记录

全部生产/测试冻结并提交44d2e39b后，首次pipeline在`test-reader-session-launch-recovery.mjs`中止：旧探针只装载`launchSourceContent`及旧section桩，遗漏PH51新增`quickHeader`成员，触发`Cannot read properties of undefined (reading 'bind')`。不是原生运行错误，也未进入构建/签名/设备部署。root的定向范围遗漏了此独立夹具，因此统一门禁发现缺口。

修补测试加载真实quickHeader/headerMeta和生产Auto几何；新增断言Quick源恰有一份顶部，Full源仅含播放区，继续检查无Scroll、禁止输入/无障碍副本。该测试已PASS，生产代码未改变；再次冻结并统一重跑。PH48/49/55独立报告见[阅读控制审计](PH48-49-55-READING-CONTROL.md)。
