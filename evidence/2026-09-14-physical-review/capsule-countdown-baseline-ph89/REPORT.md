# PH89 倒计时数字基线补修

用户于2026-09-16反馈：自动翻页胶囊左侧倒计时数字偏高。审计基线 Harmony `9f0d5c69` / Core `bf4949531`，开始时 clean。手机最后安装 `20260915T151630Z-356f150e-e58cc5c3`；VM 当前 `20260915T170116Z-973aa937-8ca702f9`。两者此处实现相同，本次没有新操作设备。

## 定位

生产 `ReaderSessionCapsuleLeading` 将 16×16 的 Text 放入 16×16 圆形并居中，只证明文字布局框中心。数字使用 `TYPE_READER_SESSION_COUNTDOWN` 的 Noto Sans SC / 900 / 8fp / 8fp行高，字体字形仍位于行框中心上方。

当前归档 Figma `capsule-figma-live.json` 的 Countdown 数字为同一字体/字号/行高，容器中心对齐；用户此前要求外圆16×16已执行。此次修正平台字形基线，不重画圆或改变原字体角色/胶囊动效。

重新读取旧 PH89 VM 原图，数字7/6/5/4的墨迹上下边界中心分别为2689/2689.5/2689.5/2689px；圆形像素中心2691.5px。偏高2～2.5px，原图密度3.5px/vp，对应约0.57～0.71vp。阈值、范围和各图读数见[像素记录](existing-vm-pixel-audit.json)，[放大图](before-enlarged.png)。这是旧图定位证据，不冒称修后新截图。

## 实施与边界

仅对倒计时 Text 添加 `baselineOffset('-0.65fp')`。当前本地 SDK `ets/component/text.d.ts:1145`说明负数向下移动内容；用fp使校正跟随字体缩放，不绑定某台手机的物理像素。圆形和文本框仍16×16，胶囊高24，字号/行高/颜色/标签/暂停按钮/倒计时节奏均保留。展开阶段和稳态均复用 `ReaderSessionCapsuleLeading`，无第二份数字布局或单独时钟。

复用 ArkUI 原生文本基线属性；没有新增字体测量引擎、字形替换或运行时逐秒测量。生产 Builder、全局字体架构约束、48 actor胶囊轨迹回归通过，日志同目录。没有为这项小幅样式修正新增仅重复实现的断言。

代码补修及本地检查完成；本轮未重新打包/安装，修后原生像素和人工视觉验收尚未完成。前次PH89布局框通过不等于数字字形居中通过，该缺陷已明确补记。
