# 当前代理与搜索修复结果（2026-09-15）

本轮已继续实施并修复代码侧定位的缺陷，最终签名包已在原 Mate 80 Pro VM 保数据安装并完成实际搜索/试读/内容搜索/返回样本。没有新的产品待决事项；平台内部错误、性能指标和真机验收必须与代码完成分别记账，不能宣称所有书源或所有反馈已全部验收。

## 已修复的代码缺口

| 范围 | 当前实现 |
|---|---|
| 代理下请求前全部被拦 | 统一HTTP/ArkWeb选路：系统有效HTTP代理负责远端DNS；PAC实际启用配置及DIRECT/排除按系统处理；当前系统网络确实承接的198.18/15合成DNS可通行；普通公网及私网准入边界保留，不改用户代理/DNS、不关证书校验。 |
| 原生WebView任务相互污染 | 独立任务surface、不可变回调身份、清理ACK；旧任务回调丢弃。6cf包early/cancel真实原生样本通过，后续未改这条生命周期；不是外网全矩阵验收。 |
| 搜索阻塞与慢源拖延 | Core搜索/前台阅读worker隔离、连续补充书源队列；前台抢占后台预解析；稳定结果对象与增量发布，去掉大对象Prop复制。已修复Publishing窗口中真实Host快速回包竞态。 |
| 有结果但不可读/切源重查 | 详情、目录、首段正文分层准入；正文失败继续可靠的同书候选；换源复用既有候选/缓存，再后台刷新。试读返回详情，再回原查询，不能直接丢到书架。 |
| 正文数组/转义残留及旧缓存 | 原始规则只执行一次，字符串/段落数组按类型准入；对象/错误JSON不作为正文。format3及受保护旧缓存刷新保留续读、书签、原数据；没有全局删除反斜杠。 |
| 作者身份拆分 | 作者字段标签与实际书源模板装饰分别处理；仅按当前源规则/版本/真实搜索事实推导@标签，原始作者和JS上下文不改；保护真实@笔名、空昵称、旧版本缓存、详情继承。SQLite只迁移派生身份索引。 |
| HTTP正文/URL | 文本、表单、multipart复用Core有界共享编码；合法空响应和缺正文HTTP错误分开；各请求阶段统一平台规范URL，跨域不带旧Host。 |
| 网络失败误当解析失败 | 所有真实request.request拒绝保留HTTP来源；已知临时错误可重试，TLS/其他非重试错误保持不可重试。JS catch后的typed必要结果可恢复真正HTTP原因，独立规则错误与后续成功请求不会被旧错误覆盖。 |
| 历史失败误降权 | 只有明确规则/响应格式/目录空/正文空事实才影响解析失败排名；旧无码失败记录保留为上次尝试，恢复正常候选预解析，保留既有成功目录/正文事实。 |
| 失败无法定位 | 安全的requestId/operationId/stage/platformCode/elapsed贯穿Host→Core→搜索失败记录；同一码后续失败仍可逐请求关联，不输出URL/请求体/认证值。 |

## 最终检查与产物身份

- Core `cfb0208f4`：3,879/3,879测试、210协议、fmt/clippy、结构漂移及C/C++ ABI检查全部通过；源码clean。
- Harmony生产 `e5483b93`：268组合同、ArkTS、隔离构建、Native与内置书源字节检查通过；构建时仅证据文件dirty。后续证据提交不倒写原构建身份。
- Native SHA-256：`425bcd200e1c9b34adedd0904f2a2a027e5253acf6060f24640d83de3c35b2d7`。
- Run：`20260915T081914Z-e5483b93-86eb9be1`，verified signed/debug，iteration/acceptanceEligible=false。
- HAP SHA-256：`04ae9321ec60af7a970c6bfdf6177d77a23e4d9729769c88a0cd64ce1c239d35`。
- [最终manifest](vm-e5483b93/manifest.json)、[保数据部署回执](vm-e5483b93/deploy-vm-6460677a198b-20260915T082126Z.json)。原产物位于同名 `.reader-artifacts/hap` immutable run。

## 最终包实际VM结果

[完整操作与原始证据](vm-e5483b93/ONLINE_RUN.md)：

- 冷启动只点书架搜索入口，输入焦点和系统键盘即出现；不用第二次点输入框。
- 鸣龙4.704秒采样已有准确关关公子；30.671秒71/109源、453组、13候选。终态598组、准确组18候选。网络状态会变化，这不是固定环境下的A/B性能跑分。
- 详情2.682秒采样进入松鹤，首章2.748秒采样正常；目录683章。正文仅验证所见首章段落，不称全书通过。
- 完整内容搜索首次输入、雨/的两次提交、实际结果List滚动通过；退回详情/换源18候选/恢复原搜索通过。
- 120秒主线程探针：117窗口/2,315样本，没有秒级停顿；搜索前30秒最大139ms，启动入口最大414ms；内容搜索期间无>50ms窗口。不能称零卡顿或60fps。
- 真实6次2300999和1次TLS2300060已分别保留请求关联；TLS明确transient=false。已修复的诊断与错误归属闭环通过，2300999更深的原生原因仍未知。

## 仍未关闭的边界

1. **源级失败不等于已证明源坏。** 本次22个失败包括6内部错误、HTTP错误、缺搜索规则/空标题、TLS、网络地址拒绝及2个Core超时；既有日志未能解释内部码的底层细因，不能承诺109个来源全可用。不得再把这些统一记为解析失败。两次超时已复查Scheduler/SDK/Host/Core回调：可见性只约束排队，未发现已派发回调被隐藏页禁止；现有日志不足以确定其具体耗时环节，见[超时核对](vm-e5483b93/reader-final-search-timeout-review.md)。
2. **性能未宣称全部达标。** 已消除本样本秒级主线程阻塞，仍观察到139ms搜索和414ms入口停顿；GPU/ArkUI逐帧与真机指标未验收。现有8/32/64MiB同步桥压力风险保留，不能用普通样本推翻。见[性能代码审计](performance-current-c56/AUDIT.md)。
3. **真机层仍OPEN。** 本轮没有操作手机、切换代理或清数据；当前既有VM网络实际搜索和试读可用，不替代每种手机HTTP代理/PAC/VPN配置验收。原15项其他视觉/全章下载等边界仍见[逐项记录](../PH77_91_RECHECK_20260915.md)。
4. **证据不改写历史。** 旧c56/968包的失败、测试加载器/取证脚本失败和Native竞态复现均保留。新修复不能倒写成旧包已通过，也不把VM安装算成用户验收。

[代理范围与原则](../PH92-PROXY_COMPATIBILITY.md)、[旧968样本与本轮新缺陷定位](vm-968a8b57/ONLINE_RUN.md)、[非重试HTTP修复](../minglong-full-assessment/repair/nontransient-http/reader-native-failure-final-handoff.md)。
