# af406 VM 在线搜索与试读样本

2026-09-15；产物 `20260915T054939Z-af406d49-11c64cbe`，签名 HAP SHA-256 `912e6682b8ea83516ca34214568d1b6ecc038c81ad86b18decabd276860487f4`。只操作此前已运行的同一 VM，targetRef `6460677a198b`；覆盖更新保留数据。手机未操作。本记录不覆盖随后作者归并、HTTP 空响应等新提交。

## 前提与最小范围

代码/本地回归已经修复系统代理、合成 DNS 路由、PAC getter、请求取消与正文准入。PH76 的 af406 早期资源实际在 6 ms 内命中，探针却把 start 之前的初始化 blank pageEnd 算入而判失败；生产代码已排除 blank，诊断器在 d4b959a7 修复。此探针计时错误不阻塞独立的 HTTP 真实链路检查，原始 FAIL 未覆盖或改写。

此次只查询已存在的《鸣龙》历史词，用现有 109 个启用源，打开首组关关公子的《鸣龙》、试读首章、返回详情、打开换源。未添加书籍、删除数据或修改系统代理。操作、布局文件和 manifest/部署回执同目录留存。

## 实际观察

- 点击历史词：Host UTC 06:00:55.436 开始，06:00:55.695 注入完成。第一次布局观察窗口 06:01:18.417–19.906，即点击后约 23–24 秒：58/109 源已完成，431 组结果，7 个源失败。关关公子的《鸣龙》位于第一组，松鹤阅读为主候选，已发现 8 源。
- 第二布局窗口 06:01:34.317–35.487，即约 39–40 秒：91/109 源已完成，687 组结果，12 个源失败；首组候选增加至 11 个。此次没有恰好 30 秒的观察点，不能改写为“30 秒完成 91 源”，也没有精确记录首个准确结果首次出现时间。
- 06:01:35.533–35.856 点击首组；06:01:37.396 已取得详情布局，显示正确作者、松鹤阅读和章节信息。只能给出该观察上界，不能把 UI 自动化命令耗时当成应用首帧延迟。
- 06:02:26.397–26.584 点击开始阅读；06:02:27.370 布局中已有首章正文、正确段落和 683 章目录总量。当前可见页没有字面 `\\r\\n` / JSON 数组包装；这不代表未显示部分或全部 683 章已逐章验收。
- 一次 Back 后恢复《鸣龙》详情页，没有跳到书架；随后打开换源，在第一次布局已显示从搜索继承的 11 个候选及松鹤“目录已就绪”。后台同时出现“正在刷新书源”，因此只证明候选即时复用，不能写成完全没有任何后续请求。
- 当前有明确绑定 target 的 DNS 样本仍为 `www.zongheng.com → 198.18.0.189`，和修前相同。此域名证实当前合成 DNS 环境仍存在，不能推定每个源的具体 IP。ICMP 的 VM 计时不用于衡量 Reader HTTP 性能。

## 本次发现，继续代码侧闭环

1. 同书作者 `关关公子` 与 `作者：关关公子` 分组不一致；还观察到作者末行 `进入作者主页 →`。交接作者身份链审计，最终 Core b2633fe91 / Host 28fb1d2d 修复；需统包验证，af406 本样本保持修前证据。
2. Reader 同进程网络日志反复出现 `Cannot read property length of undefined`，不能按出现次数推导失败请求数，不能直接归因为坏源。HTTP/URL/Cookie/NAPI 编码链已审计；真实 C++ NAPI + Rust 编码桥接在 Node 的空文本及非空编码样本通过，但不能替代 Harmony 原生绑定。a1075bb5 补充受限阶段和源文件行号诊断，等待下一包准确归因。
3. 有一次“platform did not return requested raw response bytes”。代码确定遗漏合法空响应，a1075bb5 修复 HEAD/204/205/304 缺省结果及空字符串的无损零字节转换，普通 200 缺失字节仍拒绝。尚不能将这个实际请求的唯一原因写成合法空响应，下一包有状态/类型诊断。
4. 只读 `ip route` 命令不存在，未得到路由输出；记录命令失败，不把 shell 退出 0 当成路由检查通过。未因此修改系统设置或启动其他设备。

## 仍有证据边界

本样本证明当前 VM 合成 DNS 环境下 HTTP 搜索、松鹤详情/目录/可见首章和试读返回可用，已不再是所有书源在准入阶段全部失败。它不证明所有代理类型、全部书源及全部章节均可用；原生 PH76 cancel、修后作者归并、后续 HTTP 异常定位、动画帧时间和真机体验分别验证。未添加新的用户决策事项。
