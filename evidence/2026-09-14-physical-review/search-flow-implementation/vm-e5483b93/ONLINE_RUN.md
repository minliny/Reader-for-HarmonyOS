# e5483b93 最终代理/搜索修复包 VM 证据

20260915T081914Z-e5483b93-86eb9be1；Core cfb0208f4；同一 Mate 80 Pro / targetRef 6460677a198b保数据更新并启动。签名verified/debug，HAP SHA-256 `04ae9321ec60af7a970c6bfdf6177d77a23e4d9729769c88a0cd64ce1c239d35`。未改网络、未操作手机。

## 实际行为

1. 冷启动后一次点击书架搜索入口即出现输入焦点和系统键盘。查询鸣龙：4.704秒采样准确关关公子已出现，21/109源、208组、4失败、3候选；15.676秒45/109、298组、6失败；30.671秒71/109、453组、12失败、13候选。以上是采样上界，不是精确首帧时刻，也不是与此前网络状态相同的A/B跑分。
2. 详情2.682秒采样松鹤，正文2.748秒采样首章深山老陵正常段落。目录683章；没有逐章读取全书。未加入书架。
3. 完整内容搜索展开达到shell/dock顶对齐，首次输入雨2.163秒采样已有正确文字、焦点和系统输入法；提交1.866秒采样见结果。系统全选替换为的，2.153秒采样输入成功，1.867秒采样见结果；滑动后实际List片段改变。计时包含命令执行/固定等待/布局读取，不能直接称输入法初始化耗时。
4. 返回按完整内容搜索→快捷内容搜索→快捷目录→关闭控制栏→详情逐层退出；原脚本三次Back上限不足，原失败保留，续跑从实际层级一次Back到详情。换源1.857秒采样已有18源；再Back两次回原搜索，查询/结果保留。全程没有选择新阅读位置或加入/删除书籍。
5. 搜索终态08:26:34 UTC采样598组、22失败；准确作者组18源，给大帝收尸3源，没有装饰@分裂。中间切到阅读和内容搜索，离屏时间不能当可见搜索吞吐指标或精确完成耗时。

## 失败身份与性能

- 22源级失败：空标题1、原生Internal6、无body400一次、404四次、空searchUrl三次、Core请求超时两次、TLS一次、无body403一次、普通403一次、禁止网络地址一次、521一次。没有把它们统称解析失败或源站坏。
- 6次2300999与1次2300060在真实Search日志均保留requestId/operationId/stage/platformCode/elapsedMs。TLS样本 requestId250/operationId85，595ms，transient=false；其余2300999为true。该证据证明原因与归属不再丢失，不能给2300999编造更深原因。2300023/无码分支由实际Core/Host回归证明，本次没有再出现，不能说已逐种设备验收。
- 120秒debug主线程采样117汇总窗口、2315定时器样本；最大414ms出现在冷启动/打开搜索入口。可见搜索前30秒最大139ms，另61/66ms两个窗口；完整内容搜索期间未记录>50ms窗口。没有秒级主线程停顿，但不能称零卡顿或60fps；渲染/GPU及真机体验仍需独立验收。8/32/64MiB同步桥接压力风险仍保留于既有性能审计，不能因该样本正常删除。

所有失败、命令、UI布局和安全数值日志保留；未复制含URL/认证头的原始private日志。构建与本地检查完整结果见上级当前状态；安装成功不替代交互或真机验收。
