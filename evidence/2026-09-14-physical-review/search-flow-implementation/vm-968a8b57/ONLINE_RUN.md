# 968a8b57 同一 VM 实际搜索与内容搜索

包：20260915T075615Z-968a8b57-e1dc5193，Core 5052ce7a6，已验签 debug；HAP SHA-256 d0c193e5a05624cf63989f4b171b4a9fbbae6cbe399ecda00b81f4f2de06e30b。保数据更新同一 Mate 80 Pro VM，targetRef 6460677a198b；没有操作手机或改网络。

- 冷启动后点击书架搜索入口，第一次搜索页面布局已有 focused=true 的 TextInput、可见 KeyboardCanvas/KeyCanvasKeyboard 及系统输入法窗口；无需再点输入框。
- 鸣龙搜索采样：4.688秒12/109源、67组、1失败，准确关关公子及3候选可见；8.690秒仍12/109；15.672秒21/109、125组、6失败；30.751秒38/109、178组、10失败。不能将这次全源吞吐说成优于c56。
- 详情2.664秒采样已打开松鹤；正文2.718秒采样显示首章深山老陵，683章目录计数，所见正文无数组和转义换行垃圾。这里只检查可见首章段落，不代表全书或所有来源通过。
- Back一次回详情，换源1.862秒采样已有18候选，再Back两次回原搜索。查询背景继续。终态于08:00:46 UTC采样为350组、45失败，准确作者组15源，爱推书君的给大帝收尸不再带装饰@且合并3源。最后失败日志08:00:35，不把终态采样时间当精确搜索完成时间。
- 45失败：36 Internal error、3HTTP404、3空searchUrl、1TLS证书、1无body403、1deadline；旧去重日志无法逐条还原36项数码。由此复现的非重试原生错误→JS catch→解析误归因已在后续Core cfb0208f4 / Harmony e5483b93修复，不能倒写本包已含该修复。
- 默认关闭的debug120秒主线程探针：118个汇总窗口、2336个50ms定时器样本。全程最大337ms发生在冷启动/搜索入口；搜索开始后的前30秒29窗口、578样本最大49ms，没有>50ms计数；返回搜索窗口有206ms停顿。没有秒级定时器阻塞证据，不等于GPU/ArkUI帧率达标，也不掩盖启动和返回时的停顿。
- 内容搜索：完整几何shell与dock顶部一致；输入雨和的，均显示系统输入法和正确输入值，提交后约1.88秒采样结果可见；原生List滑动后首批章节片段发生变化，可继续滚动。单本已缓存正文样本，不是首次键盘精准延时或满负荷帧率证明。

## 原始失败保留

本包生成前整包门禁先发现后台生命周期状态必须先更新，恢复顺序后又发现换源测试data-URL加载器未支持多行导入；均修复且完整268组通过，先前失败未产包。内容搜索脚本先使用非法中文采集文件名，再因accessibilityText未出现在dump的text字段无法选按钮；改用ASCII文件名、现场相邻控件几何、系统全选剪切后完成，没有把脚本错误计成应用失败。

原始UI、输入与滑动命令、失败脚本输出均保留。reader-network-and-loop.log只保留网络/定时器行并去除URL，未归档包含完整PERF URL/请求头的原始private日志。
