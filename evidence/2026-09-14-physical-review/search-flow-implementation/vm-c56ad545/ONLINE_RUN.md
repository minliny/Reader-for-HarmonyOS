# c56ad545：请求编码修复后的真实 VM 搜索与试读

本节只绑定 `20260915T064548Z-c56ad545-5a0768e2`、Core `c28f0f792`、原 VM `6460677a198b`。签名 HAP SHA-256 `bd7ad1724c1a85f9fdfd460a1b8b4904d77c04b838abb3324e9b5c2c992a159d`，同包保数据安装、启动通过。Core 最终 3860/3860、210 协议、clippy/drift/ABI 通过；本次 HAP 的 Harmony 门禁、ArkTS、签名/内容校验见 build.log。仍是 iteration，Harmony 证据文件 dirty，非用户验收。

没有重启 VM、修改系统代理/DNS、安装到手机、清数据、添加书籍或修改源配置。沿用既有合成 DNS 网络；此包没有重新读 DNS，前一同网络 af406 样本已记录 `198.18.0.189`。这不是全部代理配置的矩阵验收。

## 实际搜索

只搜索一次《鸣龙》。相对点击“搜索”的时间均为布局采样完成上界，不是精确首现或网络完成时间。输入框在此前进入搜索页时已取得焦点。

| 采样上界 | 已处理源 | 已返回组 | 失败源 | 准确作者组 |
|---|---:|---:|---:|---|
| 4.682 秒 | 19/109 | 117 | 3 | 已见《鸣龙》/关关公子/66书吧-起点，3 候选 |
| 8.679 秒 | 28/109 | 350 | 3 | 3 候选 |
| 15.693 秒 | 40/109 | 363 | 3 | 6 候选 |
| 30.685 秒 | 66/109 | 520 | 8 | 11 候选，搜索仍在继续 |

随后试读返回时搜索已结束：682 个相关组、16 个失败源、准确作者组 15 个来源。该时刻只证明最终状态，未采到精确完成时刻。不能拿旧 6cf 编码失败时较快结束的 30.746 秒与本包做性能提升比值；旧包有许多请求在发出前失败。

“作者：关关公子”前缀产生的分裂已消失。但最终布局仍有爱推书君（优）的 `@关关公子`、`@给大帝收尸` 独立组，已再次交回代码审计，不把这次结果称为所有合并问题已关闭。

## 详情、正文与返回

在实际列表点击“关关公子”结果，2.674 秒采样时已经进入详情，选中松鹤阅读、简介和章节预览可见；没有人工选择其他来源。点击“开始阅读”，2.718 秒采样时首章“深山老陵”正文可见，总目录计数 683。首屏未见字面量 `\\r\\n`、数组包装残留；没有遍历整个章节或 683 章。

一次 Back 返回原详情，而非书架。点击“更换书源”，1.933 秒采样时已列出 16 个来源，66书吧和松鹤显示“目录已就绪”，其他已发现来源也在；同时显示后台刷新，不声称完全没有新网络请求。关闭换源再 Back 返回原《鸣龙》搜索结果和原查询内容。

这些是已测样本的 UI/流程事实。没有直接记录66首章被拒的原因，故不从“列表66、详情松鹤”反推具体解析故障。约20秒连续布局采样的首章文字保持稳定，但不是逐帧录屏或触控延迟测量。

## 同进程日志与剩余边界

PID 2810 的 Reader 日志没有旧 `lengthundefined` / TypeError / raw bytes 类型错误。c56已将 UTF-8/表单/multipart 全部接到共享有界编码器；本地真实 C++NAPI/Rust 编码与生产请求方法回归另见 PH92-http-empty-response-audit.md。此实搜不逐条识别每个 POST 的成功状态，不能用 GET 源成功代替每个 POST 的验证。

400、403 缺正文现已明确保留源站 HTTP 状态。原生码出现 2300999、2300023、2300003、2300052、2300060；2300999 是平台未映射聚合错误，本次没有下层 CURL 细码。原进程所有级别既有日志仅 Reader/JSAPP，无 NetStack 诊断；没有为了凑原因重复搜索。部分源缺 searchUrl、空标题；个别站点空响应、证书错误、地址准入拒绝也分别保留，不能把 16 失败源一律叫坏源。

后续代码审计另外证明原请求 URL 只验证不序列化、跨域重定向仍携带旧 Host，已在 6f486cfa 修复并有修前失败/修后通过的生产链回归。该改动不属于本包，且不能将本次 2300003、2300999 或 HTTP400 自动归因于它。真机代理、所有实际来源、持续帧率与用户验收继续独立记账。
