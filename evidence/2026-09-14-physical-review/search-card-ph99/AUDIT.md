# PH99 搜索结果封面与行高审计

当前源码基线 f716c6b5，用户未指定现场包；未操作设备、未修改生产样式。本次先依用户此前“样式先对照 Figma/Make，描述后让我决定”约定给出具体调整对照。

## 当前实现与参考

- SearchPage.ets:801：单列 List/LazyForEach，无固定 ListItem 高度。:1305 SearchResultCard 整行顶对齐，手机封面60×82，平板68×92。
- :1344 右侧 Column(space:5) 包含书名、作者、最新章、两行简介、可换行的书源/计数/在架标签。典型完整信息与单行标签约119.5vp，高于手机封面37.5vp，加底padding14.5后底部留白约52vp；每多一行标签约增加21vp。数字按源码声明计算，不是真机测量。
- :1390 长书源标签最大为右列100%，额外有右margin6，后两标签容易换行。390宽右列约282，320宽约212。
- 归档 Make 搜索 Version 5 的 App.tsx:113–168 同样定义手机60×82、平板68×92、gap12、竖向padding14；旧示例仅原书源标签与计数，后来 PH63 统一标签样式并加入在架标签、允许换行后，实际信息量更高。没有依据声称当前封面尺寸抄错了设计稿。
- 参考路径：../2026-09-13-current-gap-register/import-search-motion-raw/make-search-source/src/app/App.tsx（相对于证据根目录）；provenance 中 fileKey=SW2sjgSEPEPuPwyR0TULyP、Version5、App.tsx SHA256=3585f71d1c69336b785e9c81a2d7634fd1a9e84a643541e5e93c98c733ee4007。未冒称再次读取了 Figma 最新在线版本。

## 上轮建议（已由本轮选择替代）

手机封面72×98.4，保留当前手机60:82比例、6圆角与原字体。上部仍为封面+书名/作者/最新章/最多两行简介；书源、计数、在架标签移到卡片全宽的底部一行，与上部间隔8；卡片上下padding统一12，左右仍18。默认字号下为固定计数/在架标签保留位置，其余宽度给真实书源名，超长尾省略；窄屏或系统大字无法容纳时自然换行，不固定整卡高度、不截断必要标签。不靠减少信息或纵向拉伸图片消除空白。

平板封面建议84×约115，并同步采用全宽标签；正常卡片按内容自然增高。实现阶段不让封面根据右侧测量反复改变宽度，避免测量回路。空最新章trim后不占节点与间距；无简介不占行；封面失败回退保持相同尺寸。

对照采用手机390宽、完整简介及长书源示例，属于浏览器排版示意，不是原生截图。原生字体与系统大字最终以实际SDK约束和后续设备层分开验证。

## 测试缺口

现有 test-search-physical-layout.mjs 只验标签属性，并误注入 gap=8/padding=12，生产为12/14，未覆盖行高与留白。实施后改用真实tokens并覆盖长短书源、缺字段、已有书架标记、不同宽度/字号及异步补全；保留实际选书身份与滚动锚点。

mockup-check.json 仅证明对照在浏览器宽320/390/736无横向溢出、封面大小与标签位置的交互更新可用，无页面脚本错误；不证明Harmony原生布局。临时无头浏览器首次在沙箱内因MachPort权限被拒，获准扩大执行边界后完成本地文件检查，无网络/真机操作。正式界面仍待用户选择后修改。

## 用户已选定：继续放大封面，与右列末行底部对齐

用户在2026-09-15明确选择“再放大一点，把最后一行也对齐”，替代此前全宽底部标签建议。实施基线Harmony `dbdd5881`；不修改搜索候选/顺序/来源/选书路径。上文等待选择仅为旧时点状态。

- 手机封面88×120.27，平板96×129.88。图片和缺图占位使用共同宽高方法，保留各自60:82/68:92原比例、ImageFit.Cover和6圆角。没有按右侧高度反向调整图片宽度。
- 外层Row底对齐；右列只有与封面等高的minHeight，无固定height或maxHeight。右列分成顶部文字组与底部标签组，用原生SpaceBetween分配它们之间的余量。
- 书名/作者/最新章节/简介原字体、字号、颜色、行高和组内5vp间距保留。空白最新章节不产生空行。简介保持原两行上限。
- 标签仍位于右侧，使用原来的文字/颜色/圆角/内部padding。改用原生Flex.space(main6,cross2)替代每个标签right6/bottom2外边距，避免最后一行带2vp空尾部的假对齐，也避免最长书源标签额外超出右列6vp。SDK Flex API12已有该能力，未自建换行算法。
- 字段较短时，标题顶边及末行标签底边分别对齐封面顶底；右侧因窄屏/较大文字/多行标签超过封面高度时，卡片自然增高，封面向下对齐最后一行，允许标题高于封面顶部。保持图片原比例，不拉伸图片，不裁掉标签。
- 不添加onAreaChange、状态回写或全量重挂载，避免搜索增量更新产生排版反馈循环。左右18/20、间距12、上下14/14.5沿用。

既有SDK探针已修正为读取生产token，覆盖320/390/736宽、日夜间、在架状态、空最新章、长书源及晚到元数据、选择原book/variants。它检查实际生成的原生布局约束和事件，不声称计算了Ace原生字形/像素。无封面分支由源码核对，未伪称已经测试加载失败图片。生产源码及专项通过，完整编译/产物另行绑定。未操作VM或真机。

## 本轮最终交付

run `20260915T151630Z-356f150e-e58cc5c3`，Harmony `356f150eaf1cfad13342cc11cc312d07d23508aa` / Core `c86b6aaec60b5c60a3a73254aac03ce6ddce4de9`，构建时两仓clean。275组Harmony检查、ArkTS、非增量编译、signed/debug Profile验签、独立manifest复验PASS。[构建](build.log)、[复验](verify.log)、[归档manifest](manifest.json)。

签名HAP SHA-256 `2d3c41c1aac1628a48d22810b6afb35a8f4df7c07368b4ccba5e0a42ca0aa69d`。唯一正式 [manifest](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/.reader-artifacts/hap/20260915T151630Z-356f150e-e58cc5c3/manifest.json) 绑定 `entry-default-signed.hap`；包内同时包含上一轮状态栏补修及本次排版修改。iteration/acceptanceEligible=false，未安装、未操作设备或应用数据；VM/真机像素、用户验收OPEN。旧浏览器提案未更新为当前排版，不将其作为本次实现截图。

## 用户授权后的真机安装

2026-09-15 15:23:53 UTC，以上同一run/签名HAP已在本轮发现并确认的USB真机完成保数据覆盖安装及启动，targetRef `b1f20b88963d`。签名前后身份一致、install PASS、launch PASS、dataPolicy preserve，见[正式安装回执副本](physical-deployment.json)。未操作VM，未做额外功能测试；PH98/PH99原生像素与用户验收继续OPEN。

最初沙箱内只读发现返回`Connect server failed`，立即停止该通道操作。经自动审批允许在沙箱外读取既有本机调试服务后，现场发现USB真机与TCP VM；仅选择USB真机。没有重启调试服务、VM或清数据。此错误是工具访问阶段证据，不作为应用失败。
