# PH112：书架打开等待与提示定位；PH108 状态栏偏色再次反馈

> 后续状态：用户明确否定本报告后半段新增的续读页面机制；该机制现已全部撤回，回到原阅读链路处理重复工作。287份完整合同及新签名包/复验通过，尚未更新真机；见 [PH113 撤回与产物记录](PH113_REMOVAL.md)。下文为历史时点证据，不是当前机制的设计依据。

用户报告 PH111 安装后，点击书架时“正在打开”窗口位于左上角，仍需等待进入阅读页，状态栏颜色仍不一致。先承认 PH111 没有证明真实首屏延迟已解决；既有 PH108 代码探针也没有证明最终状态栏合成正确，不以检查数量代替现象闭环。

当前 Harmony `5fb96de4` + PH104–111 工作树、Core `bf4949531`；最新写入手机 run `20260916T051233Z-5fb96de4-8e7c0eb0`，signed SHA-256 `a8e17167d2a160a5aad71f0987f54abf96cd3111b22ed5cbc4b50cf1878e863f`。上轮流水线自动启动因锁屏拒绝，没有成功部署回执；本条反馈是用户后续操作现象，不改写过去失败事实。

## 审计边界

- 首屏：点击事件、目录/进度/正文、Core worker 队列、数据投影与Unicode映射、窗口布局准备、隐藏测量/标题/Unicode探针、持久化首屏与路由提交；查实际串行工作和受语料放大的成本。
- 状态栏：全仓写入点、ReaderWindowCoordinator 时序/所有权、ThemeHost、隐形阅读器、系统显隐、AppStorage发布、根窗口背景、safe area/状态带坐标、主题顶部渐变和纹理/兄弟弹层合成。
- 提示：实际父子 Stack 布局、通用 align 属性以及 Phone/Tablet 的容器尺寸。

用户补充：在线书《测试书甲》，打开约等 2 秒；状态栏进入阅读页后一直不一致。当前先做代码审计、已有证据与本地生产方法探针。源码、探针、包、真机与用户验收分层记账。

## 当前代码侧结论与修复

- 提示定位已经确定：Index 根 Stack 是 TopStart；旧组件根节点只是窄 Row，通用 `.align(Center)` 只改变组件自身绘制区域中的内容对齐，不能改变它在父 Stack 中的位置。本机 SDK 声明与实际组件探针均确认这一差别。改为满屏 Center Stack 包裹提示 Row，并删除调用处误用的 align；`opening-layout-green.log` 通过。探针验证实际生产 Builder 的父子结构和尺寸属性，不冒充设备像素验证。
- 在线续读两次 progress.get 和首次 progress.update 中，Core 对同一不可变 basis 各投影正文两次，确认是重复开销；正在复用同一文档并保留完整兼容校验。代表正文的本机基准不能代表《测试书甲》或手机，尚不能据此解释 2 秒。
- 已纠正旧队列假设：当前 Core 有普通、延迟敏感和独立阅读/书源/搜索通道，不能将所有首屏等待归因于单一 worker 阻塞。预备章节通常复用正文；registryReady 时来源版本来自内存，不能笼统声称每次都重新下载或读取全量书源。

## 稳定状态栏偏色：已审计、已排除与最小取证范围

已检查全仓原生颜色写入、窗口所有权、ThemeHost、切前台重放、窗口重建、旧 ACK、失败/新请求顺序、可见性切换、所有主题色值、ReaderShell/Index/LRE/ReadingSurface 的安全区与绘制层。生产 Coordinator/LRE 本地时序、SDK Observer 及主题真值回归通过。

代码侧确认：原生系统栏只有 ReaderWindowCoordinator 一处写入；当前 reading 主题同时用于原生请求和状态带；8 个主题的 paperStart/paperEnd/statusBackground 相等且不透明，因此排除渐变首尾色不一致。ThemeHost 在 reader 所有权下不会覆盖颜色。未找到可以解释本次稳定偏色的新确定异步竞态。

尚不能确定：手机上原生状态栏实际合成色、前景色、状态带真实坐标及覆盖关系。只有纸纹两个主题的正文带纹理而状态带为纯色，用户当前主题未知，不能当成本次原因；Index 仅扩展底部、子 Shell 扩展顶部的组合也不能仅凭 SDK 声明断言必然错位。既有测试用 stub 直接保存请求属性，AppStorage 也是期望值，均不是实际系统读回或合成画面。这是之前测试覆盖的盲点。

因此申请一次最小、只读取证：用户停留《测试书甲》稳定偏色页时，仅取当前屏幕、当前 Reader 窗口/节点布局和已有 Reader 日志；不翻页、不改设置、不模拟重现、不重启或重装。目的是对照实际状态栏与紧邻纸面的像素、窗口/状态带矩形及当前主题，判定实际覆盖层。旧安装授权不扩大为新取证授权；等待用户明确同意后才重新发现精确目标、检查就绪并取得租约。当前尚未执行 PH112 真机操作。

## 本轮实际取证与用户范围更新（替代上述待取证状态）

用户已明确授权并停留阅读页。本轮仅完成一次当前屏幕、布局和已有 Reader 进程日志读取，未翻页、点击、改设置、重启或安装。证据为 `current-reading.jpeg`、`current-layout.json`、`current-reader-hilog.txt`。原目标设备，回执仅本地保留，系统已启动。VM 未使用。

当前纸质主题的系统 `StatusBarBackground_Row_0` 为不透明 `#FFEEE4D0`；截图顶部均值约 `(239,228,208)`，正文空白边缘约 `(226,215,195)`。正文叠有纸纹/光照，系统栏覆盖成了纯基色，这是持续偏色的实际合成原因。App 根 origBounds 为全窗口，不能把裁剪后的可见 bounds 当作顶端错位。详细定位见 `CHROME_ROOT_CAUSE.md`。修复统一复用 `ReaderPaperBackground`，在同一全窗口原点绘制状态带纹理与光照，只有有效阅读主题的原生状态栏使用透明底；未知主题/应用页保持不透明回退。源码和本地 SDK/所有权测试通过，修复后手机像素仍 OPEN。

用户随后明确拒绝任何“正在打开”窗口，并要求所有入架书（包括未读书）点击即展示正文。因此之前“修正提示居中”的实现被删除，不作为最终方案。当前实现范围改为每本书一个真实的原生测量入口页：本地/在线新书在入架确认前准备首章；已读书保存实际续读页；存量书架在点击前补齐；持久化页在重启后必须核对 Core 内容/位置/布局后才准入，图片通过已有 Host 图片缓存复原。没有新增通用缓存淘汰算法，没有 32 本近期书限制；复用 AtomicFile、既有 Core/Host 和分页器。准备不能写阅读进度、计时、朗读或音量接管。点击仅同步选取已准入页并挂载实际正文，已移除等待浮窗。

全书入口的代码与回归尚在整合；不能把“已有真实页时同步路由”说成全部书、冷启动、失败书源或设备耗时均已验收。字体/排版变化、导入/换源事务、连续滚动退出和晚到回调必须一起验证。存量书架补齐失败保留书与数据，单独提示可重试，不用空白页伪造成功。

## 全量整合第一轮

正式流水线第一轮 `hap-build.log`：291 组合约通过；SDK 编译失败（ETS 对象展开、实际组件导出名接线），没有发布可部署HAP。修复保持真实生产编译门禁，不以Node测试替代SDK通过。

追加审计找到：强制横屏退出后系统恢复书架方向，使横屏续读页失效；正在补真实窗口几何收敛和退出前重新测量。冷缓存入架还须先注册实际UIContext字体；已给Coordinator统一资源准入回调，Index按上下文保存已注册描述符，避免每次进度提交重复注册。按书名限定的在线替换规则以前只从shelf取标题，导致未入架和入架后正文投影不一致；使用同一Core snapshot已有source_books标题作未入架回退、保留shelf改名优先级的修复和回归正在执行。

## 第二轮整合完成项与证据边界

- 强制方向退出已补真实窗口属性/安全区的逐帧收敛、晚到几何最多3次原生重测；继续显示正文、只写一次Core进度。SDK没有任意未来旋转完成承诺；有界超时保留阅读页。系统方向未增加该等待。
- Store 14、Coordinator 13、Owner后台flush四路径回归通过；设置持久化失败不撤销原入口，成功才失效重建；冷缓存先完成真实字体注册。没有写未读书进度来伪造准备状态。
- Core标题投影修复通过正式check-local：3895测试通过、1手动项跳过，210 conformance与C/C++ ABI通过。正式NAPI与SDK smoke（51项/416断言）通过。最新raw SO SHA256为 `73e6f206b5fa0b51c98a727f3591f4e2dee8461256421dee92efd61a8c0c9465`，已由父任务复制并核对Host `entry/libs/arm64-v8a/libreader_core_napi.so`。`entry-title-native-receipt.json` 中hostSynchronized=false描述子任务交接时点；本段记录后续同步。当前buildId不能区分同dirty HEAD的内容变化，必须同时绑定该raw SHA和回执中Core源码hash。
- 本地同字节改名重复导入需先更新已有shelf标题再测量，避免按书名替换规则失配。已提交后页面准备失败仅报告可重试，保留Core书籍和Host资源；丢add回复时核对实际标题及全部写入元数据，不能仅凭旧identity误认提交。真实Gateway覆盖成功/失败×正常/丢回复四路径，以及旧行存在但请求未落地的第五路径。未知结果保留恢复日志和资源。
- 本轮仍只是源码/本地回归与HAP层工作。存量书架首次补齐、设置或几何变化重建期间，未准入书会显示准备/重试状态；这不是所有设备上的零等待验收。失败书源必须先取得真实正文，不能以空白页面、旧位置或隐去加载提示代替要求。真机尚未安装本轮代码，状态栏修复像素、每本书点击耗时、旧闪退/动效的整机复测与用户验收保持OPEN。

后续整合记录：`hap-build-final.log` 在合约通过后被SDK拒绝一处catch unknown直接throw；已复用errorMessageOf转换为Error，并覆盖Error/string/Host message三类输入。独立只读审查指出add回包期间窗口/字体变化仍可能撤销提前准备页；本地和在线均已增加提交后的实际入口再确认，提交后的失败保留成员事实、资源并报告准备可重试。`hap-build-complete.log` 在真实Store→Owner→Index组合测试中发现旧事件期望缺少第二次准备，已同步为新生产时序，同时保留Store持久化、预取不失效和ready断言；该14场景测试重新通过。两次失败均保留日志，没有选择或发布对应HAP。

## 最终产物与未完成验收层

- 正式迭代流水线 `hap-build-verified.log` 成功：293组合约、ArkTS完整编译、打包、签名和内嵌书源完整性通过；独立 `hap-pipeline verify` 成功，见 `hap-verify.log`。编译存在既有警告，不记录为零警告构建。
- run：`20260916T124744Z-5fb96de4-35300470`；不可变manifest（原始证据仅本地保留）。源码构建指纹 `35300470f2fce4e4a795cb23b80f9a29b75e9ee955da385505be62fceacca202`，742项输入。
- signed HAP SHA-256：`f3fd89ff3192165ed36e6b8aff38b4418e7355b6fba7c6f2bd729152674dbe9b`；本地debug Profile，签名复验true。Harmony commit `5fb96de4cf322c9b4f35c558051ba1fea5196ad5` dirty，Core commit `bf49495317798f68b98928712eb6e106af02bed1` dirty。保留用户原有工作，无提交/清理/重置。
- 产物manifest中的vendored Core raw SHA与本轮Core源码回执、Host复制件一致，独立复验通过。此包为iteration，`acceptanceEligible=false`，不能替代clean acceptance。
- 本轮设备仅有之前授权的只读截图/布局/已有日志；未安装此新包、未操作VM、未新增设备交互；书籍/进度/设置未在设备上被本轮修改。新包的全书架初次准备、实际点击耗时、旋转/字体变化后入口、纸纹状态栏像素与用户验收均OPEN。点击路径同步无Core/文件/排版等待是源码和本地方法证据，不是手机0毫秒承诺。

## 用户随后要求真机安装（2026-09-16 21:57 CST）

用户明确“安装到真机”后执行，未扩大为交互测试。重新核对当前源码742项、controller359项、workspace15项与manifest无差异，Core两源码与本轮native回执hash一致；signed包独立复验PASS。

首次沙箱内HDC发现返回Connect server failed，当即停止该路径；确认本机8710已有服务监听后，以获准的外部权限只读发现，唯一USB真机Connected，boot.completed=true。没有重启HDC/设备或改变VM。官方inspect确认既有debug应用与包身份一致、preservesData=true，详见 `install-inspect.log`。

官方流水线保数据覆盖安装、安装后身份核对、启动均PASS。原目标设备，回执仅本地保留，run `20260916T124744Z-5fb96de4-35300470`，signed SHA `f3fd89ff3192165ed36e6b8aff38b4418e7355b6fba7c6f2bd729152674dbe9b`。完成时间UTC `2026-09-16T13:57:15.417Z`（北京时间21:57:15）。无卸载/清数据；没有额外翻页、打开书籍、修改设置或截图。

原目标设备，回执仅本地保留；部署回执明确 `install=PASS`、`launch=PASS`、`dataPolicy=preserve`；功能交互、状态栏最终像素、书架真实点击耗时和用户验收仍OPEN。本节替代前段“新包未安装”的时点状态，不改写当时证据。
