**Reader 全量修复执行与 VM 回归记录（2026-09-11）**

## 继续执行中（2026-09-11，本节为最新状态）

上一轮提前结束，不代表全量请求已完成。本轮继续修复并测试；原52项仍全部保留。最新已安装候选为 `20260911T033330Z-35f2f99a-03670f1f`，signed debug、181组检查、Native solver235/motion538/renderer373/barrier177通过；512份源文件、223份控制文件初检无差异。已保数据安装并正在集中回归，下面b2b32102及其结果均为历史迭代，不能代表现工作区。

本次新增落实：Native上传中其他槽位及同槽替换的就绪竞争、失败上传旧像素准入；两个物理页保留退出视野内容及按revision缓存provider；chrome完整测量/布局缓存；当前截图等待mounted内容revision；滚动量化和恢复布局期间禁止提前保存；固定每个物理槽位的静态截图标识；PixelMap读取移到worker并在回到UI后重新校验capture代次/指针/布局/生命周期；Native以O2优化编译；去掉控制栏中段清晰度平台和自动morph慢尾（仍保持单一轴、原actor轨迹与手势续接）；离屏静态页不再创建无用的整页Canvas。

当前已取得的新增VM证据（各包独立，不合并为全通过）：

- `8305fd6b` 平移next最长UI任务10.949ms，previous两段16.189/6.335ms；正文Build/Destroy为0。仍是短样本。
- `8305fd6b` 重复滚动重进曾跳1192px；`48bfaca2` 补修后8次重复重进均0px，另一次短滚退出约1px。限当前TXT/当前布局，非图片或跨章全验收。
- `48bfaca2` 在分钟边界翻页复现当前页空纸；`cb19557b` 固定物理槽位截图ID后，同类晋升后分钟边界样本中正面/背面/底页有正文，source/destination同为10:28。
- `48bfaca2` 拷贝在UI上40.457ms、转换28.119ms；`cb19557b` 拷贝确在worker（5.751ms）、转换2.134ms，UI绑定0.003ms。上传13.288ms含等待，不等于GPU完成。
- `cb19557b` 一帧49.031ms中guest调度仅6.188ms、未调度42.843ms；截图组件销毁的完成回调仍21.237ms guest调度成本，推动本次移除无用Canvas。不得据此宣布全程无长帧。
- `48bfaca2` 目录打开/展开/收起/关闭10.755/9.560/8.512/18.995ms；`cb19557b` 设置对应14.318/8.930/9.384/12.385ms。749cd950七模块实际操作与端点通过，设置中间采样有内容；其外观39.079ms、Auto Page 28.658ms（含GC）、关闭设置29.760ms仍驱动03670f1f继续修复端点重复分配和跨实例chrome测量，最新包复测中。

QuietHold的两种精确注入尝试均不可用：独立ELF依赖库不可访问，官方uinput暂停自身子进程被guest拒绝；没有绕过权限。这些样本不能充当真正无MOVE的停住按住验证。真机/120Hz/物理触控到显示、温升功耗仍遵守“后续VM测试”限制。详情以 execution-status.json 的 continuingExecution 和各候选 evidence 为准。

## 此前迭代（以下不是最新候选，含旧状态表，仅为历史记录）

已执行生产代码修复，并将规范签名包保数据安装到现有 VM；**尚未完成全量验收**。本轮未使用真机，也未重启、替换、清理 VM。原 52 个编号全部保留，不能将实现、结构检查和少量交互样本混作 52 项全部关闭。

当前签名候选：`20260910T191155Z-35f2f99a-b2b32102`；[manifest](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/.reader-artifacts/hap/20260910T191155Z-35f2f99a-b2b32102/manifest.json)；HAP SHA-256 `a98e3a682ae3678a471c6e93b36d62911767bb279ff23c019c5b75e2cc59faa6`。180组Harmony检查，Native solver235／motion538／renderer373／barrier159，ArkTS／Native编译、签名和独立包复验通过；iteration，`acceptanceEligible=false`。Mate 80 Pro VM（`127.0.0.1:5555`）保数据安装启动通过，[部署回执](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/.reader-artifacts/hap/20260910T191155Z-35f2f99a-b2b32102/deploy-vm-6460677a198b-20260910T191350Z.json)。504份源文件和221份控制文件在[逐文件检查](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-vm-followup/repair-validation-b2b32102/current-source-identity.json)时全部匹配工作区。机器记录见 [execution-status.json](./execution-status.json)。

本轮实际新增修复：页头四类文字测量缓存；页槽以普通 provider 和原始数值 revision 隔离 MOVE 时的复杂对象复制；时钟／胶囊几何完整纳入 Native 纹理失效；模式切换正常 SurfaceLost 不再禁用仿真。离屏邻页截图已改为等待绘制完成，双向中间底页空白的VM复验通过。当前页新revision截图等待绘制、滚动恢复等待实际布局帧及补偿START对齐偏移均已落实；另外修复了保存滚动模式冷进入时，非响应数组未触发分支切换导致分页外壳与点击失效。已有修复和其他任务的首屏 ArkUI SDK 普通方法修正、返回路由修正均保留；未修改 Core 业务实现。

**已获得的 VM 证据与限制**

- `7b396cce`：仿真→滑动→覆盖→仿真→无动画，各模式实选控件与双向正文往返通过。平移 100 次交替操作（50 次 next、50 次 previous）逐次正文比较通过；它不是连续同方向各100次，也不是快速吞吐测试。
- `9abcb3e8`：同一 TXT 页对的两段平移 trace，接触期间最大 UI 帧任务 next **17.776ms**、previous **31.582ms**；整个窗口峰值 **29.395 / 46.940ms**。两段正文 ReadingSurface Build/Destroy 均为0。仍有长帧，不能宣布完全跟手或上一页延迟已消失。
- `4d55b276`：EPUB 控制栏打开／目录展开／收起最大 UI 帧任务 **20.169 / 17.546 / 12.842ms**；原审计对应样本为75.263／26.886／25.346ms。样本的书籍、冷热状态及宿主调度不完全相同，只用于定位改善趋势，不能算严格 A/B、p95/p99 或用户观感验收。
- `48433b48` TXT 页对仍记录明显页面参数复制／更新成本，推动后续 provider 隔离。`9abcb3e8` 的 readingRoot 最大更新降到3.523ms，但反向正文更新和收尾长帧保留待查。
- `7b396cce` Native 两向确有曲面、背面文字和随分钟更新的时钟；同时发现底页空纸，视觉判为未通过。正确终态正文不能证明中间纹理正确。
- `74ac9927` 双向中间底页空白已修复并捕获真实底页正文；`b2b32102` 滚动冷进入、短滚动、退出重进可用。但滚动恢复坐标仍有10px差值（-308→-298px），超过本轮8px阈值，R01继续部分完成。
- `9abcb3e8/simulation-smoke` 和 `final-sim-*` 的仿真身份已撤回：当时实际仍为覆盖。原始资料保留，排除理由在该目录 `validation-exclusions.json`。另排除落在桌面的截图、控制层尚未关闭的滑动、UITest drag 前置长按触发的文字选择。
- QuietHold 精确注入器已在执行前 dry-run 阶段遇到 VM 动态库依赖错误，没有授权请求或触点注入；真正无新 MOVE 的 Hold 仍 OPEN。普通UITest drag有前置长按、同坐标MOVE不等于静默Hold。

所有时间是 App `OnVsyncEvent` 墙钟或嵌套组件工作，非真实屏幕帧时长／触控到显示；guest 调度时间也不是宿主物理 CPU。不同书籍及不同包的样本不合并为一个通过结果。原始证据见 [VM 更新](../2026-09-10-page-turn-vm-followup/VM_FINDINGS_AND_REPAIR_UPDATE.md)。

| 编号 | 代码状态 | 本轮落实 | 仍须完成 |
|---|---|---|---|
| F01 | 既有约束保留 | 初始化不 swap、可见阶段不提交透明清理帧。 | 真实显示逐帧回归。 |
| F02 | 已实现／本地检查通过 | release/隐藏/清理分离，清理完成后才释放输入。 | 显示交接证据同 N08。 |
| F03 | 已实现／本地检查通过 | 代次首帧门禁；同代次进入收尾保持可见。 补修模式切换正常 SurfaceLost 被误判故障，VM 重入通过。 | Surface 重建与迟到回执 VM 验证。 |
| F04 | 已实现／本地检查通过 | 固定事务 source/destination；业务成功后旋转槽位。 实际卷页发现的离屏空底页已修复；文本页双向中间帧通过。 | 双向提交/回滚逐帧验证。 |
| F05 | 已实现／本地检查通过 | 平移/覆盖两个无条件固定槽位，角色及数据更新。 本轮去掉逐帧全页复杂参数复制；有效 slide trace 的 ReadingSurface Build/Destroy 为 0。 | 对比 VM 正文 Build/Destroy；见 P08。 |
| F06 | 既有约束保留 | 主题不透明底和统一呈现阶段保留。 | 主题、模式切换和实际呈现。 |
| F07 | 已实现／本地检查通过 | 静态快照排除高亮；ArkUI 字符矩形同步 Native 动态着色。 | 高亮与卷页、终帧的像素连续性。 |
| F08 | 待运行验证 | 全模式/失败/生命周期验收矩阵完整保留。 | 已保数据安装；平移/覆盖/仿真/无动画同章双向样本及滚动冷进入/普通退出路径已测。滚动精确恢复10px FAIL；全模式失败、完整生命周期和全视觉矩阵仍OPEN。 |
| F09 | 既有约束保留 | 阴影混合保留目标 alpha，GL 状态测试通过。 | 合成链 alpha 和局部闪烁实测。 |
| C01 | 部分完成 | 生产 Panel 同帧/端点/布局缓存、准备任务避让已实现。 | 需新包 trace 证明中段长帧改善，不能宣称用户停顿已消失。 |
| C02 | 已实现／本地检查通过 | 统一帧采样缓存、端点及几何依赖失效。 | VM 分配与实际布局成本对比。 |
| C03 | 部分完成 | 稳定 owner 布局对象，避免等值复杂参数反复触发；保留真实文字重排。 | 真实尺寸变更仍存在，须按新 trace 决定是否进一步拆分固定布局。 |
| C04 | 已实现／本地检查通过 | 目录使用稳定行锚点与一帧一次必要滚动修正。 | 深滚/反向/再抓及迟到数据的视觉连续性。 |
| C05 | 已实现／本地检查通过 | 稳定行 key，逐行差异更新，移除全量 JSON key。 | 大列表、筛选排序、删除与异步更新 VM 回归。 |
| C06 | 待运行验证 | 保留现有非空中段和轨迹；尚未调整清晰度平台/自动曲线。 | 先测修复后长帧，再对照 Figma 判断慢尾和平台调整，不能盲改为另一套动画。 |
| C07 | 部分完成 | 移除重复裁剪，控制交互暂停无关邻页测量/快照。 | 剩余滤镜及宽高布局需 UI/GPU trace 归因。 |
| C08 | 语义待明确 | 保留最近书签原选择规则，未把歧义直接当缺陷修改。 | 确认产品最近书签是否指当前 scalar 距离。 |
| N01 | 已实现／本地检查通过 | 去掉 24ms 回放；VSync 使用最新有效样本。 | 真实输入接收、消耗、提交及显示时间轴。 |
| N02 | 已实现／本地检查通过 | 内部/边缘起手和再抓统一相对抓取锚点。 | 镜像拖动、停住、反向的 VM 轨迹。 |
| N03 | 已实现／本地检查通过 | 原子 endGesture 消费最后位置；迟到 MOVE/重复 UP 被拒绝。 | 设备事件顺序回归。 |
| N04 | 已实现／本地检查通过 | 同事务拖动到收尾保持 Native 可见。 | 同版本首/终帧及回滚实测。 |
| N05 | 已实现／本地检查通过 | 80–320ms 手动收尾、已到端点立即结束；提交前按已提交姿态再抓。 | 不同时间点反复再抓和位置/倾角连续性。 |
| N06 | 已实现／本地检查通过 | 事务纹理保持，视觉切换不提前失效；提交后旋转。 | 跨章、失败及回滚资源回收。 |
| N07 | 已实现／本地检查通过 | 隔离测量 lane；当前/请求方向/另一方向捕获；帧间让出，保存前驱观察。 邻页截图等待绘制完成，VM 双向空底页已修复。 | 冷/热页与图片场景起手延迟及主线程预算。 |
| N08 | 部分完成 | 区分 buffer submit 与 ArkUI 内容准备；保留不透明终帧并校验 revision。 | 仍使用 postFrame 调度交接，没有显示 fence；固定帧数不构成实际上屏证明。 |
| N09 | 已实现／本地检查通过 | 字符矩形、Canvas 正文混色和 Native UV 高亮；64 矩形上限与显式降级，无全页重复上传。 | SDK glyph 矩形、中文字体/图片/夜间、GPU shader 及像素实测。 |
| N10 | 部分完成 | 书沟暗带随 tau 连续归零；有效纹理链保留。 时钟和胶囊几何进入 Native 纹理身份，分钟更新在空闲刷新；VM 实际卷页时钟已更新。 | 颜色空间/过滤/端点差异未测；SetThemePaper 仍是测试入口，生产拒绝无纹理绘制，未新增未经验证的 fallback。 分钟边界source／destination时钟仍可能差1分钟（最终包03:19／03:20），不能标时钟一致性已关闭。 |
| N11 | 部分完成 | 每轮最多上传一张，输入之间可重新准入；纹理捕获串行并让帧。 | PixelMap 拷贝、转换、上传/GPU 分段耗时与总帧预算。 |
| N12 | 已实现／本地检查通过 | V2.1 普通程序化 320ms、积压 rapid 180ms 上限；移除旧隐式平移动画注册。 | ≥4.5页/秒是包含保存及交接的目标，尚未实测达标。 |
| N13 | 部分完成 | 固定纹理/网格/≤3 draw，静止按住无重复画帧；销毁清高亮数据。 | 总进程/Canvas/PixelMap/临时副本峰值和长跑趋势；真机温升功耗另验。 |
| N14 | 已实现／本地检查通过 | 输入所有权、消费代次、固定纹理身份、上传 surfaceEpoch、再抓尺寸/表面校验；清理失败保留 owner。 | 乱序与生命周期已补本地用例；旋转/退出/重建 VM 矩阵。 |
| N15 | 已实现／本地检查通过 | SLOTS_COMMITTED 只做逻辑提交，TERMINAL_RELEASED 后恢复后续绘制。 | A 清理/B 首帧交错、丢回执及 Surface 重建实测。 |
| P01 | 已实现／本地检查通过 | 未就绪保留首指与意图，ready 后按当前位置重新锚定，不补播旧位移。 | QuietHold 无 MOVE、冷页、抬手及取消 VM 回归。 |
| P02 | 已实现／本地检查通过 | UP 最终位置先归入手势，再区分点击与拖动。 | 稀疏/仅 UP 越阈值和快速反向实测。 |
| P03 | 已实现／本地检查通过 | 纵向上一页位移映射到正确 X；最终 offset 连续。 | 平移/覆盖纵向路径实测。 |
| P04 | 已实现／本地检查通过 | none 取消立即复位并释放；不遗留 settlement。 | 系统 CANCEL/模式切换回归。 |
| P05 | 已实现／本地检查通过 | 权威边界在入队前判断，不能抵消反向有效请求。 | 首尾和净目标组合 VM 回归。 |
| P06 | 已实现／本地检查通过 | 两固定物理槽位，promotion 保留反向页；静态 snapshot id 位于内层。 本轮去掉逐帧全页复杂参数复制；有效 slide trace 的 ReadingSurface Build/Destroy 为 0。 | 实例挂载计数和快速往返。 |
| P07 | 已实现／本地检查通过 | 未知写结果先串行查询 Core；目标确认提交、原页确认回滚、未知保持并仅重试读取。 | 本地 lost reply/foreign/unavailable 通过；真实慢存储/故障注入未验。Core get/update 同 latency FIFO，无新造 source-switch transactionId。 |
| P08 | 部分完成 | 冻结页面投影并以 revision/provider 更新固定槽位，避免首次复杂数组残留。 | 旧 6/11、44% 往返首行差异尚未在新包复验，根因和关闭仍 OPEN。 |
| P09 | 已实现／本地检查通过 | none 也复用预准备页面；原页保留直到权威保存确认。 | 点击到正文 ready、跨章和持久化延迟实测。 |
| P10 | 已实现／本地检查通过 | 请求方向优先、准备可中断，前驱二分及已发现前缀续算。 | 热/冷、同章/跨章的双向延迟对比。 |
| P11 | 部分完成 | 冷指针续接及旧请求绕过 lease 的确定性路径已修。 | 不能用单一修复解释全部用户反馈；仍按住但无新 MOVE 的真实响应待 VM。 |
| P12 | 已实现／本地检查通过 | 冻结事务两页/布局，metric 索引和页起点二分，稳定数据 provider。 本轮去掉逐帧全页复杂参数复制；有效 slide trace 的 ReadingSurface Build/Destroy 为 0。 | 正文 build/layout、GC、分配的 VM 对比。 |
| P13 | 已实现／本地检查通过 | DOWN 起统一首指 lease；rapid、Auto Page、音量键及迟到准备不能绕过。 | 7b包完成100次交替正文检查；连续单向各100次、活跃指针来源冲突及rapid吞吐仍待验证。 |
| P14 | 已实现／本地检查通过 | 首次 owner 授予做完整准入，MOVE 走轻量检查；每手势一个 watchdog。 | 输入、内存及超时恢复实测。 |
| P15 | 已实现／本地检查通过 | 统一单调时间，异常时间重建速度样本基线。 | 设备事件/VSync 时钟关联，不把本地注入当现实异常发生率。 |
| R01 | 部分完成 | 同条目滚动也更新段内位置；停滚、模式切换、退出采样。 补齐冷进入内容版本订阅，按布局帧和实际row y恢复。 | VM冷进入与退出重进可用；短滚恢复仍差10px，超过本轮8px阈值。须补精确字形锚点/量化稳定性及多次往返，不能标精确恢复通过。 |
| R02 | 已实现／本地检查通过 | 单个 awaitable 保存 drain，失败保留页面/重试；晚回执校验生命周期与事务序号。 | 最终包普通短滚→退出→重进可用；退出失败提示/重试、慢写和停止回调设备回归仍OPEN，精确位置偏差见R01。 |
| R03 | 已实现／本地检查通过 | 图前/图内稳定 fragment+fraction 锚点补偿；合并图片批量更新并保留同时手动滚动。 | 图在视口上方/内部/跨章，SDK 自动布局与用户滚动组合实测。 |
| R04 | 已实现／本地检查通过 | 额外手指不覆盖首指或结束首指事务。 | 与原生 List 手势组合的多指 VM 测试。 |
| R05 | 已实现／本地检查通过 | 滚动返回上一章直接请求章节尾部，最终保存实际首可见位置。 | 超长章耗时、尾部实际视口及恢复验证。 |

**本轮结束状态**

最终包仿真两向中间帧均有真实底页正文；滚动冷进入和控制打开恢复；平移设置已恢复并完成最后4次正文往返。03:24:39（北京时间）probe正常退出，随后确认本任务VM锁不存在；最终为测试书10%、第2/10页、控制栏关闭。见 [释放记录](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-vm-followup/repair-validation-b2b32102/VM_RELEASE.json)。未使用真机；未卸载、清数据或重启/替换VM。最终504源文件／221控制文件全部匹配，见 [末次身份检查](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-vm-followup/repair-validation-b2b32102/final-source-identity.json)。其后目标和工作区可被其他任务改变。

残余必须明示：滚动短滚退出恢复10px偏差仍FAIL；最终包分钟边界Native source03:19／destination03:20仍未一致；上一页接触段31.582ms长帧来自9ab诊断样本，未证明最终包消失。精确QuietHold输入器加载失败，原声称的停住才起动未完成运行因果闭环。

**共同验收边界与后续出口**

- 五模式双向、冷／热／跨章、QuietHold、最终UP、CANCEL、多指、每方向100次、10次再抓；成功／回滚／失败／未知写结果，退出／前后台／主题字体缩进／旋转／Surface 重建仍按各自证据关闭。
- 控制栏七模块、深列表／异步数据、p约.2/.5/.8停住反向及快速抓回仍需逐帧与trace关联。真实宽高布局、滤镜、清晰度平台与自动曲线慢尾尚未全关闭，不擅自替换既定 actor 轨迹。
- P08 必须在原第6/11页、44%条件记录权威scalar、布局版本、内容identity及往返正文；另一书籍第1→2→1或100次同页往返均不能替代。
- 原生底页空白已有74ac双向文本样本修复证据；动态高亮、字形矩形、图片／夜间、颜色空间／过滤、alpha和gutter仍需实际像素验证。N08没有实际显示fence，postFrame和eglSwapBuffers均不能作为送显回执。
- rapid ≥4.5页/秒是全链目标，180ms仅动画上限。PixelMap/纹理上传、GC、总内存、空闲CPU/GPU、千次长跑仍需预算与趋势测量。
- 120Hz、低端机、物理触控到显示、温升功耗与最终用户验收不由VM代验；遵守用户已释放真机、后续VM测试的限制。

当前合同修订见 V2 文档的 V2.1；旧24ms回放、600/240ms时长、不可再抓及swap等同presented的叙述已被覆盖。共享工作区大量改动保留，按manifest快照逐文件识别，不能把其他任务变化归为本任务成果。

