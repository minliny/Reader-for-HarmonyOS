**覆盖录像离线补查，2026-09-10**

仅分析本地原片，未连接真机或 VM。原片 `reader-turn-cover-20260910-0030.mp4` 共 725 个解码帧、116.405722 秒、868×1920；身份见 `video-inventory.json`。

录像开始时控制栏已关闭，正文可见。约 PTS 18–26 秒包含翻页活动，能看到连续的横向中间位置；随后长时间保持正文页面。概览不包含控制栏展开、收起全过程。输入日志记录四次 next 点击、四次 previous 点击、两次反向慢滑；记录下发成功并不等于每次业务提交已确认。

`cover-frame-pts.json` 保存原始 PTS。`cover-body-signalstats.txt` 和 `cover-body-metrics.json` 对全部 725 个解码帧取正文区域 x=60、y=200、w=748、h=1530 的 Y 亮度。PTS 与 ffprobe 相差最大约 0.4 微秒，来自输出小数精度。正文平均 Y 范围 214.477–220.323，没有捕获大面积全白或全黑正文样本；平均值不覆盖局部闪烁、单帧漏采样或显示端合成异常。

约 18–26 秒有 377 个原始样本，样本密度 49.21 fps；相邻 PTS 间隔 p95 为 22.189 ms（nearest-rank），最大 441.423 ms。活动窗口含动作间等待，且系统录像按画面变化采样；这些数值不能直接写成应用帧耗时、掉帧率或触摸延迟。后续两个约 22–24、24–26 秒窗口样本密度分别为 56.91、58.26 fps，也仅描述录像采样。

收尾后抽样正文首行与本次覆盖前的“张浚竟然辩驳不得”一致，页码回到 6/11。没有覆盖后的独立布局/进度/内容身份快照，因此不据此完成往返准确性验收。此前平移前后首行变化仍是独立未解释项，不能用覆盖结果冲销。

ffmpeg 的 null 输出曾报告派生时间基准 DTS 舍入警告；分析采用滤镜中的原始 PTS，并与 ffprobe 的 725 帧逐项核对，不采用 null 输出的时间基准。原始视频未修改。
