# 翻页与控制栏：完整修复与验证总账

更新于 2026-09-11T11:07:57.599092+00:00。原52项全部保留，执行中；当前只使用已有Mate 80 Pro VM，未使用真机。代码、具名VM样本与用户体验验收分开记录。

## 当前修复包

已保数据安装 `20260911T105306Z-35f2f99a-98686c3b`，signed SHA-256 `15732ceb85f082964569da08bb7ecd71b6799bdb005b358d73d5db7859740783`，本地签名及debug Profile复验通过。181组Harmony、Native235/538/373/199、ArkTS与Native编译通过；安装前512源码/223控制文件指纹无漂移。Harmony HEAD35f2f99a、Core HEAD6b2a9d87；共享dirty保留，本轮未修改Core业务。iteration，不是clean acceptance。

[manifest](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/.reader-artifacts/hap/20260911T105306Z-35f2f99a-98686c3b/manifest.json) · [部署回执](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/.reader-artifacts/hap/20260911T105306Z-35f2f99a-98686c3b/deploy-vm-6460677a198b-20260911T105505Z.json) · [当前证据](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-vm-followup/repair-validation-98686c3b) · [机器总账](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/execution-status.json)。

## 本轮补齐的关键证据

- 四种分页效果双向“移动400ms后保持按住6s”通过；1.2、3、5秒采样正文像素无漂移，松手目标与反向恢复按原文片段验证。公开输入工具支持-k保持，之前“无法注入”的判断已纠正；未把1.2秒后静止样本冒充首事件触控延迟。
- 仿真40页前进/40页后退，无中途界面读取，各精确40次成功提交，规范净目标正确。首事务开始到最后完成7.564/7.172秒，即5.288/5.577页每秒；不含首事务前准备，不是显示帧率或触控到像素。
- c097四模式稀疏UP、两/三指双向共24步，8次Home/前台恢复通过；文本2前2后规范页回归及具名卷曲样本通过。
- 多指+音量键到滚动章尾额外跳章已修：程序滚动不再继承旧手动标记；两/三指精确视口基线与反向恢复通过。音量键OFF、自动翻页STOPPED保留。
- e3自动翻页按住12秒跨8秒到期点、目录部分行10次与尾部3次往返通过。5da800次不同页跨章往返、b901暖冷图片模式切换、642图片原生卷曲等原包证据全部保留。

## 性能和验收边界

c097带反复dumpLayout的仿真40页10.435/11.456秒受测试读取开销影响；当前单调时间取证将事务完成和观测时间分开。普通平移/覆盖/无动画同类40页约6.2秒，净目标均正确。连续Native短轨迹UI峰依次81.562、51.084、21.243、49.966ms，仍有长任务；不能选最低一次宣布全部流畅。控制栏设置/目录历史10段隔离轨迹峰6.611–11.302ms，旧GC长帧仍保留。

N08仍无实际显示fence：postFrame、内容revision与EGL swap只证明各自阶段。物理120Hz、触控到屏幕、温升功耗、真实系统CANCEL与原P08《绍宋》34章6/11页44%的旧scalar/layout证据，均不能由VM自建书替代。036同章800次PSS未持续上升，但heap增加，不能宣布无泄漏。

## 原52项完整状态

| 编号 | 代码 / VM | 已完成与证据 | 尚未关闭 |
|---|---|---|---|
| F01 | RETAINED / PARTIAL | 可见期间不提交透明初始化/清理帧；Native GL 状态与交接回归通过。 | 642修复后图文正面/背面/相邻页卷曲像素通过具名样本；文本页曲面中间样本已取得；全主题/图片/实际显示逐帧矩阵仍需独立证据。 |
| F02 | IMPLEMENTED / PARTIAL | 隐藏、清理、释放输入分阶段；清理失败不让新事务覆盖，199项 barrier 检查通过。 | N08没有实际显示 fence；不能把调度回调计数视作送显确认。 |
| F03 | IMPLEMENTED / PARTIAL | 首帧按代次准入；模式切换正常 SurfaceLost 不再禁用仿真；90be四模式各3次前后台同PID正文恢复通过。 c097四模式各2次Home/foreground正文一致。 | 强制 Surface 销毁/重建及迟到回执的完整 VM 故障矩阵。 |
| F04 | IMPLEMENTED / PARTIAL | 事务 source/destination 固定，成功后旋转物理槽；已修空底页与物理截图ID，双向文本中间帧有正文。 | 642修复后图文正面/背面/相邻页卷曲像素通过具名样本；图片、回滚、提交各阶段实际像素覆盖仍未齐全。 |
| F05 | IMPLEMENTED / PARTIAL | 平移/覆盖固定 a/b 两物理节点；退出视野内容保留，provider 按 revision 缓存；8305平移正文 Build/Destroy=0。 | 快速交错与全部内容类型下的实例计数；离屏截图组件生命周期不能混算为当前正文重挂载。 |
| F06 | RETAINED / PARTIAL | 不透明主题底与统一呈现阶段保留；四分页效果的实选切换和正文往返已验证。 | 642修复后图文正面/背面/相邻页卷曲像素通过具名样本；夜间/图片/字体与模式交错的完整像素矩阵。 |
| F07 | IMPLEMENTED / OPEN | 静态快照排除高亮；ArkUI 字符矩形同步 Native 动态着色。 | 高亮与卷页、终帧的像素连续性。 |
| F08 | VALIDATION / PARTIAL | 五模式基础样本；90be稀疏UP/多指与12次前后台；5da四模式800次不同页跨章往返；1368图文正文通过但Native图片像素失败，642真实图文卷曲像素修复通过；b901滚动/分页暖冷锚点往返通过。 | 失败/未知写结果、旋转、强制Surface丢失和全视觉矩阵仍未齐全，不能宣布全验收。 |
| F09 | RETAINED / OPEN | 阴影混合保留目标 alpha，GL 状态测试通过。 | 合成链 alpha 和局部闪烁实测。 |
| C01 | PARTIAL / PARTIAL | Panel 复杂时钟对象改为普通决策状态；界面只观察进度/可见度/占用标量和稳定路由；90be七模块操作端点通过。 | c0隔离复测设置/目录10段UI任务最大值6.611–11.302ms；历史GC长帧保留，整体p95/p99、GPU和实机仍未测。 |
| C02 | IMPLEMENTED / PARTIAL | 同帧几何、完整端点、字体顺序缓存；页眉跨离屏实例64项LRU；生产缓存失效用例通过。 | 47fc进一步复用设置页同帧actor几何/paint及选项数组；尺寸、反向、开关实时状态的SDK闭包回归通过。完整分配峰值与实机p95/p99仍未测。 |
| C03 | PARTIAL / PARTIAL | 稳定语义路由避免每帧触发整个模块分支，按标量发布actor变化，保留真实文字重排。 | 剩余宽高布局/GC预算继续保留；90be七模块最大值分操作列在trace报告。 |
| C04 | IMPLEMENTED / PARTIAL | 实际List可见行和行内比例定位；1368深目录10次往返；e3第40章部分行fraction0.642857，10次Full/Quick往返≤4px；139–150章尾部3次往返通过。 | 排序和筛选往返已验；中途反向/再抓及异步数据交错仍需分别验证。 |
| C05 | IMPLEMENTED / PARTIAL | 稳定行key与逐行差异通知，内容变化和视觉进度分离。 986实测150章升降序、149章筛选各3次Full/Quick；书签新增/切tab/删除与清空筛选通过。 | 远程迟到数据/下载状态异步变更和动画中删除的组合尚无匹配VM证据。 |
| C06 | IMPLEMENTED / PARTIAL | 已消除20%–80%清晰度平台；自动展开/收起使用完整smoothstep轴，保留actor轨迹、320/260ms时长与手势速度续接。 | 749采样中段非空；当前包p约.2/.5/.8、反向、10次再抓及完整连续画面仍待采集，不能标全程流畅。 |
| C07 | PARTIAL / PARTIAL | 去重复裁剪、交互避让邻页任务；普通与静态离屏页不再创建无用整页Canvas。 | 余下滤镜、宽高布局及GPU完成耗时没有完整预算；两次GC长帧仍保留。 |
| C08 | SEMANTIC / OPEN | 保留最近书签原选择规则，未把歧义直接当缺陷修改。 | 确认产品最近书签是否指当前 scalar 距离。 |
| N01 | IMPLEMENTED / PARTIAL | 24ms回放已移除，Native VSync取最新有效样本；90be双向UI窗口峰值15.914/14.414ms。 | 这是guest UI任务短样本，不是输入到屏幕延迟或120Hz验收。 |
| N02 | IMPLEMENTED / PARTIAL | 内侧/边缘起手与再抓共用相对锚点；双向正常拖动已执行。 986四分页效果双向400ms移动后保持按住6s，1.2/3/5s采样正文像素无漂移，松手规范页目标与反向恢复通过。 | 公开uinput已确认支持移动后-k保持；此前“无法注入”的判断已纠正。暖页静止窗口已测；冷页首次输入、精确最后MOVE至首像素、反向再抓仍分开验证。 |
| N03 | IMPLEMENTED / PARTIAL | endGesture原子消费最终位置；90be四模式仅UP位置跨阈值双向各1例通过，额外手指不结束首指。 | 真实乱序/CANCEL仍以生产逻辑用例为主，未覆盖所有系统事件。 |
| N04 | IMPLEMENTED / PARTIAL | 同事务拖动到收尾保持 Native 可见。 | 同版本首/终帧及回滚实测。 |
| N05 | IMPLEMENTED / OPEN | 80–320ms 手动收尾、已到端点立即结束；提交前按已提交姿态再抓。 | 不同时间点反复再抓和位置/倾角连续性。 |
| N06 | IMPLEMENTED / PARTIAL | 事务纹理及代次保护保留；已补上传中同槽/异槽pending重检与失败纹理失效，199项检查通过。 | 跨章及失败回滚资源寿命的完整VM统计。 |
| N07 | IMPLEMENTED / PARTIAL | 请求方向优先；缓存槽不再无条件空等帧；物理当前截图与revision门禁；被淘汰邻章按需读取；仅离屏截图树同步解码并校验图片状态。 | 1368空白→03降级失败→642图文卷曲正反/相邻像素通过且仿真选项仍可用。更大图片、冷解码耗时分布与布局/主题交错待补。 |
| N08 | PARTIAL / OPEN | 区分 buffer submit 与 ArkUI 内容准备；保留不透明终帧并校验 revision。 | 仍使用 postFrame 调度交接，没有显示 fence；固定帧数不构成实际上屏证明。 |
| N09 | IMPLEMENTED / OPEN | 字符矩形、Canvas 正文混色和 Native UV 高亮；64 矩形上限与显式降级，无全页重复上传。 | SDK glyph 矩形、中文字体/图片/夜间、GPU shader 及像素实测。 |
| N10 | PARTIAL / PARTIAL | 时钟与胶囊几何进入纹理身份，分钟空闲刷新；修复晋升后角色ID错绑导致当前快照空白。cb19557b同类分钟样本10:28一致。 | 642修复后图文正面/背面/相邻页卷曲像素通过具名样本；当前候选双向分钟边界/图片/夜间像素矩阵待补；颜色空间/过滤/端点暗带与fallback仍未完整实测。 |
| N11 | PARTIAL / PARTIAL | Native worker拷贝、UI完成准入重检、O2、单离屏树更新、最多一张被指针打断的完成位图复用。557文本/图文卷曲样本通过；e3连续24步正文Build/Destroy=0。 | 连续Native短轨迹UI峰：b90181.562ms、55751.084ms、e321.243ms、c09749.966ms；不能宣布长任务全部消失。仍需GPU完成、分配峰值和物理120Hz证据。 |
| N12 | IMPLEMENTED / PARTIAL | 普通程序化320ms、物理手势80–320ms；积压已完成点击30ms；Native交接199项通过。986无界面读取的40+40各精确40次提交，首事务开始至末完成7.564/7.172s（5.288/5.577pps），规范净目标正确。 | 统计不含首事务开始前准备，不是逐显示帧或触控到屏幕速度。c097反复dumpLayout的10.435/11.456s保留为受观测影响结果，不能直接归因动画。 |
| N13 | PARTIAL / PARTIAL | 固定纹理/网格/有界LRU；036同PID800次往返PSS466157→455814KiB，过程无持续PSS上升。 | Heap allocated 96298→110428KiB；不得宣布无泄漏。全分配峰值、120Hz、长时温升功耗未测。 |
| N14 | IMPLEMENTED / PARTIAL | 统一代次、surfaceEpoch和lifecycle；worker迟到拒绝入队并释放资源；90be四模式前后台同PID恢复通过。 | 旋转、强制Surface丢失/重建、进程终止等仍需分别验证。 |
| N15 | IMPLEMENTED / OPEN | SLOTS_COMMITTED 只做逻辑提交，TERMINAL_RELEASED 后恢复后续绘制。 557补齐Auto Page倒计时、控制背景点击与PERF耗时的单调时钟。 | A 清理/B 首帧交错、丢回执及 Surface 重建实测。 |
| P01 | IMPLEMENTED / PARTIAL | 未就绪保留首指与意图，ready 后按当前位置重新锚定，不补播旧位移。 986四分页效果双向400ms移动后保持按住6s，1.2/3/5s采样正文像素无漂移，松手规范页目标与反向恢复通过。 | 公开uinput已确认支持移动后-k保持；此前“无法注入”的判断已纠正。暖页静止窗口已测；冷页首次输入、精确最后MOVE至首像素、反向再抓仍分开验证。 |
| P02 | IMPLEMENTED / PARTIAL | 最终UP先归入手势再判点击/拖动；90be四模式仅UP变更坐标的next/previous共8例通过。 c097同类双向UP稀疏位置回归通过。 | 极稀疏反向轨迹与真实系统CANCEL组合仍待覆盖。 |
| P03 | IMPLEMENTED / PARTIAL | 纵向上一页位移映射到正确 X；最终 offset 连续。 | 47fc四模式均已实测纵向上一页→横向下一页，跨章正文往返通过；动态逐帧触控对齐仍需显示证据。 |
| P04 | IMPLEMENTED / PARTIAL | none取消立即复位；四模式切换及none正常正文往返通过。 | 真实系统CANCEL没有可用公共注入入口；代码取消路径用例通过不等于设备全验收。 |
| P05 | IMPLEMENTED / OPEN | 权威边界在入队前判断，不能抵消反向有效请求。 | 首尾和净目标组合 VM 回归。 |
| P06 | IMPLEMENTED / PARTIAL | 两个固定物理页、promotion保留反向页，固定current-page-a/b截图ID；036四模式800次正文稳定检查通过。 | 036是十页同章扫动，5da四模式800次不同页长跨章往返已通过；还需异常交错实例统计。 |
| P07 | IMPLEMENTED / OPEN | 未知写结果先串行查询 Core；目标确认提交、原页确认回滚、未知保持并仅重试读取。 | 本地 lost reply/foreign/unavailable 通过；真实慢存储/故障注入未验。Core get/update 同 latency FIFO，无新造 source-switch transactionId。 |
| P08 | PARTIAL / PARTIAL | 冻结投影/revision/provider和稳定物理槽已落实；原案例重新核实为真机《绍宋》第34章6/11页44%。 | 原案缺少当时权威scalar/layout identity，且真机已释放。VM自建书原文oracle不能替代原案关闭。另发现的被淘汰前章卡住已独立修复。 |
| P09 | IMPLEMENTED / PARTIAL | 无动画复用预准备页并等权威保存；补齐缺失邻章读取，保留原页和待处理方向。 | 点击到ready、慢保存和跨章的完整时间分布仍待测；各包功能样本分开记录。 |
| P10 | IMPLEMENTED / PARTIAL | 请求方向优先，前驱前缀续算；补修前章缓存淘汰后prepare直接放弃的缺口，去重读取并拒绝旧代次/旧页/旧生命周期结果。 | 5da原失败25→24末→25已通过；5da四模式各100 next+100 previous均通过，冷远程章时延尚未全测。 |
| P11 | PARTIAL / PARTIAL | 冷指针续接及旧请求绕过 lease 的确定性路径已修。 986四分页效果双向400ms移动后保持按住6s，1.2/3/5s采样正文像素无漂移，松手规范页目标与反向恢复通过。 | 公开uinput已确认支持移动后-k保持；此前“无法注入”的判断已纠正。暖页静止窗口已测；冷页首次输入、精确最后MOVE至首像素、反向再抓仍分开验证。 |
| P12 | IMPLEMENTED / PARTIAL | 冻结两页/布局、索引二分、稳定provider；8305平移当前正文重挂载计数0，90be控制观察依赖进一步拆分。 | 全部内容类型的GC/分配/UI+GPU预算未齐全，不能由单一trace推出全模式跟手。 |
| P13 | IMPLEMENTED / PARTIAL | 分页/原生List共用首指占用；b9016s静止按住+音量键通过；e312s静止按住跨8s自动翻页到期点通过；c097修复多指后的程序滚动误继承手动章尾标记。 c097两指/三指+音量键到章尾后不再额外跨章，精确基线与反向恢复通过。 | 两/三指+音量键和静止到期已有具名通过；公开uinput现支持移动后保持，分页四模式已验。真实系统CANCEL、额外指提前UP与所有输入源交错未全覆盖。 |
| P14 | IMPLEMENTED / PARTIAL | 首次授予完整准入、MOVE轻量检查、每手势一个watchdog；90be普通/稀疏/多指路径及前后台通过。 986四分页效果双向400ms移动后保持按住6s，1.2/3/5s采样正文像素无漂移，松手规范页目标与反向恢复通过。 | 公开uinput已确认支持移动后-k保持；此前“无法注入”的判断已纠正。暖页静止窗口已测；冷页首次输入、精确最后MOVE至首像素、反向再抓仍分开验证。 |
| P15 | IMPLEMENTED / OPEN | 统一单调时间，异常时间重建速度样本基线。 | 设备事件/VSync 时钟关联，不把本地注入当现实异常发生率。 |
| R01 | PARTIAL / PARTIAL | 滚动scalar量化/内边距坐标/恢复布局门禁；分页切换按规范页界；c097补齐已测前缀中的空白与头部分隔符，对齐完整页索引。 | 78b四距离-3/0/0/4px、8次重进0px；03图片短滚重开1px；b901暖/冷模式切换各5页双向往返通过。超长章冷定位耗时、字体及章尾仍待补。 |
| R02 | IMPLEMENTED / PARTIAL | awaitable保存drain及晚回执校验，恢复期间停止回调禁止提前覆写；78b短滚退出及8次重进通过。 | 真实慢写/失败提示/重试/退出中止仍以生产逻辑用例为主。 |
| R03 | IMPLEMENTED / PARTIAL | 图片fragment+fraction补偿与并发手动位移保留；fraction已统一正文内边距坐标。 图片资源到达时更新行内容身份，修复LazyForEach复用Blank。 | 03图文短滚退出显式重开偏差1/1/1/1px通过。字体变化、章尾、慢解码和多图片同时到达仍待补。 |
| R04 | IMPLEMENTED / PARTIAL | 额外手指不结束首指；滚动500ms长按边界；c097在程序化滚动准入时明确结束旧手动标记，避免抵达章尾额外跳章。 | 两/三指+音量键和静止到期已有具名通过；公开uinput现支持移动后保持，分页四模式已验。真实系统CANCEL、额外指提前UP与所有输入源交错未全覆盖。 |
| R05 | IMPLEMENTED / OPEN | 滚动返回上一章直接请求章节尾部，最终保存实际首可见位置。 | 超长章耗时、尾部实际视口及恢复验证。 |

[此前执行全文](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/EXECUTION_HISTORY_THROUGH_20260911_1105.md)及各包原始失败证据保留。

<!-- PRE_EXECUTION_AUDIT -->

**实施前审计原文（保留问题定义和历史证据，以下旧状态不代表本轮执行结果）**

范围：本任务原始透明帧／页面槽位方案、随后发现的仿真跟手问题、其他翻页模式遗漏、最新控制栏展开／收起卡顿，以及全部尚未完成的验证和性能项。本报告是当前源码与本地证据的审计、待实施方案；未修改业务代码，未重新使用真机。后续已完成部分同版本 VM 性能取证，尚非全量验收。

最新扩大审计与逐步实施出口见 [扩大审计及执行方案](./EXPANDED_AUDIT_AND_EXECUTION_PLAN.md)。新增旧请求绕过活跃指针的双向代码复现、Auto Page／音量键仲裁缺口、已核对的热页编译更新链、重复 admission 和异常时间戳问题。总账现为 52 个跟踪条目，包含已修约束、缺陷、成本和待验证项，不能当成 52 个已复现 bug。2026-09-10 后续已完成现有 VM 的包身份核对、4 段控制栏与 4 段滑动 trace。运行更新与边界见 [VM 取证及修复更新](../2026-09-10-page-turn-vm-followup/VM_FINDINGS_AND_REPAIR_UPDATE.md)；下文早期录像/测试记录仍按各自范围保留。

**1. 当前结论与证据边界**

- 用户本次现场没有看到翻页闪烁，但看到控制栏展开／收起中途卡顿、停留。后一个问题仍未关闭。
- 三段原片已取回。平移和覆盖原片能看到持续中间位移；正文亮度检查没有捕获大面积黑白空帧。不能据此宣布所有模式、所有显示帧都无闪烁。
- 现有录像没有完整覆盖控制栏展开／收起；不能把其现场停顿归因于某个已实测的长帧，也不能以录像没录到为由否定用户反馈。
- 源码确认：控制栏存在重复几何／裁剪计算、真实尺寸逐帧变化、目录列表同步滚动，以及清晰度平台和自动曲线慢尾。前三项是需要测量的性能成本；后两项是明确的时间／呈现规则，不能混为同一种掉帧。
- 本地执行探针再次复现：边界请求抵消反向操作、最终 UP 被误判成反方向点击、none 取消后未释放占用、滚动退出未等待在途保存。探针提取生产方法并替换外围依赖，不是设备复现。
- 仿真主动延迟、起手追赶、重复隐藏、不可再抓取等问题仍在。前几轮解决了部分透明提交路径，没有完成完整跟手优化。
- 本次平移往返前后同为 6/11、44%，但正文首行不同。这是可见锚点差异；尚不能判定是初始恢复对齐、分页身份变化还是翻页提交错误。

状态口径：**代码已修**只指相关代码路径；**未修／部分**指当前仍有明确机制或本地复现；**待验证**指症状、成本或设备后果缺少匹配证据；**已落实约束**指当前已有实现、仍需回归。P1 为影响交互或进度一致性的必修项，P2 为性能、观感或工程完善项。没有把某轮 PASS、构建成功或未复现自动转换成视觉验收通过。

**2. 源码、构建与录像身份**

此次抽样源码的 Harmony HEAD 为 `35f2f99a8d635615475fde75120bf8629f944565`，工作区有大量未提交改动；Core HEAD 及每个抽样文件的 SHA-256 记录在 [current-audit-source-snapshot.json](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/current-audit-source-snapshot.json)。不能仅凭 HEAD 认定两个包相同。

录像预检绑定的 runId 为 `20260909T144521Z-35f2f99a-cb6bfc9f`，签名 HAP SHA-256 为 `7fc3af9adc82c33a5ebf3a5193c6bd6844677eeccc118080e64b60376f4911ac`。本地包复验、签名及设备元数据检查通过，未独立读取已安装包全部字节核对。该包是 iteration，`acceptanceEligible=false`。本次抽样 57 个源码文件，56 个与该构建输入相同，`ReaderControlAppearanceContent.ets` 不同；当前外观模块细节不能直接当作录像包的精确实现。

报告结束前的二次指纹检查发现 `LocalReadingExperience.ets` 被并行任务更新，记录在 [audit-source-drift-check.json](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/audit-source-drift-check.json)。上述 56/57 是初次抽样时的匹配数，不能当作更新后的当前匹配数。已对变化文件重查 Native 重复隐藏、current 快照、双回调释放及提前恢复下一事务的方法，机制仍在；其他模式探针末次复跑的 8 个缺陷情景也全部仍复现，核心准备／保存／取消／滚动方法未因并行修改修复。分项报告中的精确行号对应各自保存的源码指纹，当前行号可能移动，实施时应同时按方法名定位。未变化的 Native／控制栏测试不因这一文件更新被无限重跑。

| 证据 | 本次实际覆盖 | 保留的限制 |
|---|---|---|
| baseline，41.634 秒／147 帧 | 已打开的快捷控制栏、进度→外观模块切换 | 没有完整打开、展开、收起、关闭过程 |
| slide，69.738 秒／588 帧 | 四次 next 点击、四次 previous 点击、两次反向慢滑 | 输入下发日志不等于全部提交回执；往返正文锚点不同 |
| cover，116.406 秒／725 帧 | 同类十次操作、持续中间位置、收尾正文 | 没有独立 after 布局／权威进度快照 |
| 平移正文统计 | 活动段均值约 213.91–220.98，无捕获的大面积黑白帧 | 不覆盖局部闪烁、录屏漏帧、显示端异常 |
| 覆盖正文统计 | 全部 725 帧均值 214.477–220.323，无捕获的大面积黑白帧 | 同上 |
| 原始 PTS | 平移慢滑段样本密度约 58 fps，覆盖慢滑窗口约 57–58 fps | 编码样本密度不是应用帧率；不能将命令耗时当触摸延迟 |

原片身份、逐帧方法及局限见 [录像清单](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/video-inventory.json)、[平移分析](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/analysis-slide.md)、[覆盖分析](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/analysis-cover.md)。真机录屏、取回与占用已结束，等待自动任务已暂停；后续测试仅使用 VM。

**3. 原始防闪烁方案完成度**

| 编号 | 原始要求 | 当前状态 | 尚需处理 |
|---|---|---|---|
| F01 | 初始化／清理不提交透明 Native 帧 | 代码已修 | 保留初始化不 swap、ClearSurface 只清后备缓冲；GL mock 通过仍不能代替显示端验收 |
| F02 | release 与 clear 分离，隐藏后清理 | 代码已修，交接部分完成 | 成功与回滚已有隐藏／释放顺序，但“旧帧已隐藏、新页已显示”的证明仍不完整，见 N08 |
| F03 | 新代次首帧握手与旧回调隔离 | 部分完成 | generation 校验与首帧事件已有；同代次转收尾仍重复隐藏，见 N04 |
| F04 | 反向槽位交换、回滚逆操作 | 方向错误已修 | 句柄逆交换不等于恢复已失效内容；见 N06 |
| F05 | 两个稳定 ArkUI 页面实例 | 未满足常驻要求 | 后续 VM 四次滑动均记录起手 Destroy 一份/Build 两份正文，收尾 Destroy 两份/Build 一份；见 P06 |
| F06 | 主题不透明底、统一呈现阶段 | 已落实约束，仍需回归 | ReadingSurface 已有主题底与呈现 phase；逻辑 phase 不能替代实际呈现回执 |
| F07 | TTS／自动翻页高亮保持动态 | 存在遗漏 | 当前页快照可能含动态高亮，而邻页排除；见 N09 |
| F08 | 全模式、成功／回滚／失败／连续操作逐帧验收 | 未完成 | 本次仅平移与覆盖的部分日间场景；其余全部继续保留，见第 8 部分 |
| F09 | 阴影混合不能降低 framebuffer alpha | 代码已修 | 已保留目标 alpha；GL-state 检查通过不等于真实整幅像素 alpha 验收 |

因此，“已经彻底无闪烁”“固定 id 已保证不重挂载”“两个帧回调已证明上屏”均不是当前可支持的结论。

**4. 控制栏问题清单与对应修复**

生产链路是 `LocalReadingExperience → ReaderControlPanel → ReaderControlRuntime`。`ReaderControlMotionStage` 当前只挂在验证页，不能只优化它后宣称生产问题已解决。精确源码定位与当前测试覆盖见 [控制栏源码审计](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/CURRENT_CONTROL_AUDIT.md)。

| 编号／级别 | 当前问题与证据 | 修复与关闭条件 |
|---|---|---|
| C01／P1 | 用户确认中段卡顿；后续 VM 已有生产 Panel 长任务：首次打开 75.263ms，目录展开重复 26.886ms，收起 25.346ms，收起中段连续超预算；进度与显示帧对应仍缺 | 在真实生产 Panel 补录并关联同一动作的进度、输入、UI 布局及呈现时间；明确是停帧、位置跳变还是清晰度平台 |
| C02／P2 | Panel 的 `frame()` 同次更新多次完整采样；端点几何、PathShape 有重复计算。部分模块已缓存当前帧，不能称全部无缓存 | 一次状态更新复用一份最终几何；独立缓存端点和同帧裁剪，明确所有失效输入；比较计算次数、分配和长帧变化 |
| C03／P1 | 逐帧改变内容实际宽高，文本、原生列表和裁剪／模糊一起更新 | 稳定只移动的内容布局，用位移和裁剪呈现；真正变宽的文字仍正确重排，保留各 actor 轨迹，不能整体拉伸文字或替换成淡入淡出 |
| C04／P1 | 目录／书签每次 progress 更新读取滚动位置并 scrollTo，同时行高、宽高和 padding 变化 | 冻结本次起始语义锚点，合并布局与滚动修正，一帧最多一次必要修正；无变化不发命令；抓取、反向、深滚后收起均不跳位 |
| C05／P2 | 目录全量 JSON 签名与含行内容 JSON 的 key，在异步业务更新时有额外重载风险 | 使用稳定行 key、数据 revision 和精确更新；结构变化才 reload；动画中到达数据不重置用户位置 |
| C06／P2 | p=0.2–0.8 时 opacity=.35、blur=2；共用曲线在 75% 时间已完成约 97.55% 路程 | 属体验规则，不等于计时器停止。先消除实际长帧，再对照轨迹调整平台／慢尾；保留中段可见内容，不能重新出现纯空白中间帧 |
| C07／P2 | 多层动态裁剪、分散模糊滤镜，以及可能重叠的页面纹理准备 | 测量 UI／GPU 成本后减少重复裁剪、合并适合的滤镜边界；交互过渡期间暂缓无关准备任务。控制动画本身没有每帧 PixelMap 捕获，不能错误归因 |
| C08／待明确 | 最近书签目前按章节距离、同章最小 offset 选择；缺少当前位置 scalar 距离 | 保留为语义核对项。只有目标确认为“距当前阅读位置最近”时才改距离算法，不能先当已确认 bug |

缓存必须按实际依赖失效。Panel 至少包括 p、visibility、motionOffsetY、width、fullHeight、quickHeight、bottomGap；不能仅按 epoch 缓存，也不能反复向缓存对象累加 offset。TTS 需保留 rate、seek／error 行等依赖；端点样本按尺寸和内容几何单独缓存。裁剪需考虑 actor、端点／当前 rect、滚动锚点、视口、密度。窗口、字体、布局变化不能命中旧缓存。

目录锚点用“稳定行 key＋行内偏移”，保留现有打开时定位与用户滚动接管规则；Quick／Full 切换不重新居中。大列表、筛选、排序、删除书签、下载状态变化和迟到数据均需验证。先采用合并修正方案；若仍超预算，再将适合的内部内容改为固定布局＋展示变换，不能未经验证冻结整个列表或缩放文字。

**5. 仿真翻页与跨模式性能清单**

以下机制、精确位置及 Native 本地测试见 [Native 源码审计](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/CURRENT_NATIVE_AUDIT.md)。

| 编号／级别 | 当前状态与问题 | 完整修复机制 |
|---|---|---|
| N01／P1 | 未修：按落后当前帧 24ms 的触摸时间线取样，主动增加延迟 | 每次 VSync 读取最新有效样本，短历史仅用于速度估计／诊断，不逐条补播。先建立无预测基线，有界重采样另行测量 |
| N02／P1 | 未修：距离页边超过 12vp 的内部起手有页缘追赶 | 按抓取时触点和当前姿态建立锚点，以相对位移驱动；中央起手、边缘起手、再次抓取统一。停止和反向不继续追旧目标 |
| N03／P1 | 未修：最后样本与进入 settle 分开处理，最终 MOVE／UP 有丢失或顺序不一致风险 | 原子 endGesture 携带最后触点、时间、seq 和提交结果；先消费最后样本再进入收尾；迟到 MOVE 不覆盖 UP／CANCEL |
| N04／P1 | 未修：同次拖动转提交／回滚时再次 opacity=0，重等首帧 | 同会话、同页面版本且 Native 已有效可见时，拖动→收尾→回滚→再抓保持可见；只有新表面、版本不匹配或失效恢复才重走首帧门禁 |
| N05／P1 | 未修：手动收尾至少 240ms，最长 600ms，收尾拒绝重新接管 | 手动收尾按剩余距离和有效释放速度决定；80–320ms 仅作待标定范围，到端点可直接完成。提交前可抓回当前姿态；保持位置、倾角连续，速度不改原有提交方向判定 |
| N06／P1 | 未修：τ 切换时真实旋转／失效纹理，回滚仅逆交换不恢复所有原始有效内容 | 每个事务固定持有 source／destination，视觉阶段只改变绘制绑定；权威提交完成后才旋转缓存、回收内容。先补完整资源所有权，再开放再抓取 |
| N07／P1 | 未修：邻页准备借用可见章节上下文；截图准入又依赖输入 idle | 将可见页和邻页准备隔离为不可变版本；准备次序为当前→请求方向→另一方向；解码／转换提前，主线程测量／截图按预算调度。不得将所有工作笼统宣称可移后台 |
| N08／P1 | 部分：FRAME_PRESENTED 实际为 swap 成功；Stage Watch 后帧回调和 Local 双回调备用分支不证明实际显示 | 区分 buffer submitted、ArkUI 内容／布局就绪、显示呈现；按 SDK 实际能力取得可验证交接，不能以固定帧数冒充上屏。保留终帧直到版本匹配；无可靠信号时保留未验证状态 |
| N09／P1 | 已确认遗漏：当前页从 live 槽捕获，可能含 TTS／AutoPage 高亮；邻页捕获排除高亮，纹理 identity 不含高亮 revision | 静态正文纹理与动态高亮分离；动态层保持 ArkUI 所有权，与翻页坐标和裁剪同步。不能靠提高快照频率把动态高亮反复烘焙进纹理 |
| N10／P2 | 代码风险／待验证：活动底页有 Native 书沟暗带，终态关闭；颜色空间链未明确；SetThemePaper 仅测试使用、生产 Host／NAPI 未接入 | 检查同版本 Native／ArkUI 端点像素、纸色 fallback 的适用条件、gamma／颜色空间和滤波；需要的主题参数必须真实下发；额外光影连续收敛，保留既定卷曲可读性。不能任意加回已废弃阴影族 |
| N11／P2 | 开销已确认、主因待测：PixelMap 拷贝、像素转换、整页上传；渲染线程批量处理待上传内容会占用输入／绘制时间 | 保留持久 EGL、固定网格、≤3 draw 的已有约束；合并最新输入、按帧预算限量处理上传，失效结果直接释放。分开统计 UI 拷贝、转换、GL 上传、GPU 和 GC |
| N12／P1 | 合同矛盾：程序化全程 600ms、单页串行，同时继承 ≥4.5 页／秒 | 普通点击／自动翻页与 rapid 分别规定节奏。600ms 串行理论上限约 1.67 页／秒；若 rapid 保留 4.5，单页全部开销约需 ≤222ms，180–220ms 仅是动画候选预算，存储与交接也计入 |
| N13／P2 | 资源限制已有，峰值／功耗未验：四 GPU 槽、单页 300 万像素、RGB8 约 36MiB 只覆盖一部分资源 | 一并统计 PixelMap、RGBA／RGB 副本、上传暂存、失效任务、EGL 缓冲的峰值；连续翻页后应回到稳定区间；检查空闲无轮询、后台停止活动帧与热状态影响 |
| N14／P1 | 协议防御缺口：Settle 未校验当前事务所有权；CommitSlots 入队前检查了 retained generation，但实际旋转前缺少同等再校验 | 所有不可覆盖命令携带 surfaceEpoch／transactionId／inputEpoch／textureRevision，消费时再检查；用 A 命令入队、B 输入先执行的乱序测试证明旧命令不改变新纹理／姿态。当前完整竞态后果未在设备复现 |
| N15／P1 | 时序仍分离：SLOTS_COMMITTED 后立即 finishSuccessful，解除输入／settlement、恢复 rapid／准备，而隐藏释放屏障尚未完成 | 区分“逻辑提交成功”和“呈现／资源所有权交接完成”；新意图可立即记录，新绘制必须满足所有权条件；A 清理与 B 首帧交叉不清掉 B、不提前复用 A 纹理 |

资源单位须区分：四张 300 万像素 RGB8 的逻辑字节数为 36,000,000 bytes，约 34.3MiB；旧约束的约 36MiB 是页面纹理预算口径，绝不是进程峰值。还需计入格式对齐和上述临时副本。

原先离线运动算法轨迹曾测得“390vp 页面中央左拖 40vp，96ms 时仍明显落后、约 128ms 才追上”。本报告不把这项旧算法样本转换成当前设备输入延迟；以 N01／N02 当前源码及重新执行的轨迹测试为实现依据，设备数值仍未完成。

固定纹理与权威进度的边界必须一起改：tracking／settling 且尚未提交，可接管；提交已经发出、结果未明，只记录下一段输入，不能假装取消写入；确认成功后从目标页继续；明确失败才回原页；结果未知先核对权威进度。Native 负责运动，业务层负责持久化，MOVE／VSync 不发存储请求。

**6. 其他翻页模式、取消与进度遗漏**

精确定位、执行探针及 Mock 边界见 [其他模式审计](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/CURRENT_OTHER_MODES_AUDIT.md) 和 [探针输出](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/other-modes-probe-output.log)。

| 编号／级别 | 当前问题、模式及证据 | 修复与回归 |
|---|---|---|
| P01／P1 | 仿真／平移／覆盖：邻页或纹理首次不就绪会锁定整段手势无呈现 | 保存 pointer、方向和意图；准备就绪且仍按住时，基于当前静止页及当前触点重新锚定，不补播等待期间积累的位移；抬手后只留一个有效意图，取消／退出／控制层接管清除 |
| P02／P1 | 所有分页：缺少越阈值 MOVE 时，UP 最终位移未先归入手势判定；本地复现右→左变为左区 previous 点击 | UP 先走最终位置更新，再分点击／拖动；tap 使用本次手势的正确条件，不能用旧 dragged 状态；快速轨迹和反向阈值都要测试 |
| P03／P1 | 平移／覆盖：本地额外探针复现纵向 previous 的 offsetX=0，以及最后 MOVE=-80、UP=-200 时呈现仍为-80 | 识别、呈现与提交共用同一轴／进度映射；若保留纵向意图，必须给出对应连续位移；收尾继承最终 UP 姿态 |
| P04／P1 | none：回拖 UP、系统 CANCEL 留在 settling／rollback，inputOwned=true；本地复现 | 无动画模式取消直接完成状态收尾，释放所有权与延迟状态；重复取消幂等；下次点击、控制栏、退出不受阻 |
| P05／P1 | 仿真／平移／覆盖：已知书尾 next 仍入 preparing，随后 previous 被 pending +1 抵消；本地复现 | 准备前判断已知边界，边界不进入净目标；清除不可执行意图。书首／书尾、连续越界、立刻反向都必须执行正确 |
| P06／P1 | 平移／覆盖存在条件分支；后续 VM 两轮双向滑动实际确认起手/收尾重挂载正文，重复同页对也发生；覆盖同构风险尚缺本轮 trace | 固定两个同级、常驻实例，角色只作数据；新页到 x=0、逻辑提交后保留旧槽，再在不影响可见页的空闲时重绑邻页；用实例／挂载计数验证 |
| P07／P1 | 跨模式：提交超时被当失败回滚，但 Core 已完成的写入不能由取消请求撤销；协议缺口已确认 | 引入事务 id、预期版本／目标锚点和 unknown 状态；超时查询权威结果，成功只提升一次，明确失败才回滚，不能猜测；晚回执、退出／恢复按同一事务恢复 |
| P08／P1 | 平移录像：往返后同页码／百分比、正文首行不同 | 单列调查：记录来源／目标内容 identity、layout revision、首可见 scalar／段落内偏移、恢复锚点、提交 id；分开复测初次打开恢复与稳定页往返，不直接归因于 P07 |
| P09／P2 | none：没有动画不等于即时响应，仍可能等待章节／分页／保存 | 测量点击→正文就绪；复用准备结果并隔离慢持久化的交互影响，不能用空白或旧页伪装已完成 |
| P10／P1 | 用户补充平移下一页比上一页跟手；固定next优先队列、previous缺索引时扫描发现前驱，且全局准备可阻塞两个方向 | 按实际意图提升准备优先级、复用版本化前驱索引并隔离可见context；镜像轨迹分别测冷／热／跨章／连续／反向，不能只调上一页时长 |
| P11／P1 | 用户明确“停止移动但仍按着”后才起动；新增 P13 已复现一条双向条件性代码链，各种场景的实际时间占比仍未确认 | 单列输入产生／接收、owner位移、实际显示与事务来源；优先修 P13，再用关闭 Auto Page 的热／冷镜像基线归因。不能用 UP 才发 onTurn 或单个新漏洞解释全部反馈 |
| P12／P2 | 17个参数更新链与线性扫描证据保留；后续 VM 滑动接触期间峰值 next 175.023/36.770ms、previous 46.555/21.000ms，含布局/Stage 成本；另捕获收尾附近及稍后 GC，单个辅助函数成本尚未隔离 | 页面投影按版本缓存、chapter metric索引、页起点二分／复用；冻结事务两页／布局并隔离高频位移；不能将脏回调次数等同全部文本layout/raster次数 |
| P13／P1 | 新增：旧rapid请求在准备完成时绕过新活跃指针启动动画和保存，next／previous均本地复现；Auto Page／音量键也缺统一指针仲裁 | DOWN即持有pointer lease，所有来源统一调度；旧队列、准备／纹理回调只能更新ready或保留意图；UP／CANCEL原子释放并按来源／epoch／页面版本消费，不能无差别丢队列 |
| P14／P2 | 新增：120次热MOVE调用120次canStartTurn与watchdog重设；完整admission结果只在首次owner阻塞分支使用 | 首次授予做完整检查，后续revision快检；watchdog基于最后活动时间低频检查；不删除生命周期和版本失效保护 |
| P15／P2 | 新增：无效事件时间回退Date.now污染lastSampleTime，正常事件恢复后速度仍无法更新；本地异常注入复现 | 统一单调时钟、独立seq、标记异常时间来源并恢复速度估计；设备实际异常发生与VSync时钟基准仍待验证，不把它当普通拖动的既定根因 |

补充的全情景代码审计、用户“仍按着”的确切语义、方向优先序和热路径排查见 [方向与按住停动审计](/Users/minliny/Documents/Reader/Reader-for-HarmonyOS/evidence/2026-09-10-page-turn-physical-b1f20b88963d/SLIDE_DIRECTION_AND_HELD_POINTER_AUDIT.md)。它扩展本报告，保留所有既有条目。平移本地热MOVE已同步更新owner，不等于屏上跟手；用户所述各种情景继续保持体验未通过。

| 编号／级别 | 滚动模式独立问题 | 修复与回归 |
|---|---|---|
| R01／P1 | 只在可见索引变化时更新位置，停滚不重新采样段内锚点 | 停滚、退出、模式切换前采样实际首可见 fragment 和行内偏移；同一批条目内短距离移动也保存正确 |
| R02／P1 | 在途保存时退出立即完成，只留 trailing 标志；失败被日志吞掉；停滚保存回调只有方向，缺少 lifecycle／rapid generation；本地探针复现提前退出 | 单个可等待的保存 drain 串行保存最新 revision，退出等待最后确认；失败保留待保存状态，不回报假成功；晚回调必须按生命周期与事务代次过滤，不能消费新一轮 rapid 意图 |
| R03／P1 | 图片从占位高度变为实际高度，没有明确的视口锚点补偿；效果待 VM | 解码前优先使用可靠比例／尺寸；更新前后用稳定 fragment＋行内偏移补偿。图在视口上方、视口内、跨章边缘分别验证 |
| R04／P1 | 多指输入没有锁定首根 pointer；本地探针复现第二指将起点从 y=100 改为500；与原生 List 手势组合效果待 VM | 锁定 active pointer id，额外手指不改起点；原指 UP／CANCEL 明确完成或取消，不把第二指跳接为同一拖动 |
| R05／P2 | 返回上一章复用逐页扫描；长章成本未量化 | 滚动定位直接使用章节布局／尾部锚点或可复用索引，避免为了滚动构建整章所有分页；测量章节长度增长下耗时、分配与首屏延迟 |

平移／覆盖也保留 N05 的“收尾再抓”问题，需要沿当前连续偏移接管；它们没有 N01 的固定 24ms Native 回放和 N02 的卷页追赶，不能把所有模式诊断成同一个原因。滚动使用独立原生列表，也不继承 Native 卷页问题。

**7. 实施顺序、共同不变量与回滚方式**

| 阶段 | 工作与依赖 | 交付出口 |
|---|---|---|
| A：固定口径与证据 | 绑定当前源码／包；为每项建立编号、基线、验收；修订互相冲突的时长／吞吐／跟手合同；建立输入与呈现统一时间轴 | 不再用错误合同、旧 Stage 或录像采样率验收新生产路径 |
| B：确定性正确性修复 | P13 统一指针仲裁；P02／P03／P04／P05／P15；R01／R02；P07 的 unknown 与事务核对。与控制栏工作可按文件责任并行，Local 协调层由单一修改者整合 | 边界、UP、取消、退出保存、晚回执注入测试通过；无状态泄漏、重复提交 |
| C：控制栏流畅度 | 生产 Panel 埋点和同帧缓存→端点／Path 缓存→目录锚点／精确行更新→减少布局和滤镜成本→最后评估慢尾／平台 | 七模块全流程、深滚／再抓／反向连续；明确长帧减少的证据；几何／可读性不退化 |
| D：仿真连续输入 | N03 原子最后样本＋N04 不重复隐藏→N01／N02 新拖动锚点→N06 固定事务纹理→N05 提交前再抓取 | 中央／边缘拖动，停住／反向、松手、反复再抓连续；不越过业务提交边界 |
| E：准备与呈现 | N07／P01／P10 独立双向准备与冷手势续接；P06 常驻双槽、P12 页面投影缓存、P14 高频输入减负；N08 真实交接；N09 动态高亮；N10 首终态光影 | 版本匹配、无空槽／透明提交／高亮冻结；失败与回滚同样成立 |
| F：性能与滚动完善 | N11／N12／N13；P09；R03／R04／R05；纹理内存、功耗及吞吐；P08 锚点调查 | 同 VM 同条件前后对比、资源有界、滚动位置稳定；设备特有指标保留未验证 |
| G：可追溯交付 | 项目唯一 HAP pipeline，当前源码／测试／Native 身份绑定；原有已就绪 VM 保数据更新、生产路径回归 | 构建、签名、安装、启动、功能、视觉、性能分别记录，不能互相替代 |

共同不变量：同一手势／动画只有一个连续进度；小把手位移百分比映射完整动效帧轴，各 actor 保留独立轨迹；点击、拖动、松手、再抓切换不重置进度。可见内容必须有匹配版本的所有者。过期的输入、上传、隐藏、清理、提交回调都检查 lifecycle／generation／transaction／revision。准备失败不废弃仍可用的可见内容；MOVE 和绘制不做进度写入；同一事务最多消费一次快速翻页目标。

每阶段保留同一复现轨迹和前后录像／trace，先以窄变更验证，出现回归只回退本阶段的代码与候选包，不重置 VM 或清应用数据。Shared workspace 现有其他改动不回滚、不合并为本任务成果；正式修改前重新确认文件状态。

**8. 全部未验证项与性能验收口径**

| 类别 | 必须保留的用例／指标 | 当前状态与 VM 能证明的范围 |
|---|---|---|
| 控制栏全路径 | 七模块；隐藏→快捷→完整→快捷→隐藏；点击、慢拖、快拖、p≈.2/.5/.8 停住反向、收尾再抓、连续切换模块 | 部分 VM 取证；首次打开、目录点击展开/收起已有 trace，其余七模块、拖动、再抓与逐帧视觉仍 OPEN。验证实际 Panel |
| 控制内容边界 | 目录／书签大列表、顶部／中部／底部、筛选／排序／删除／迟到数据；朗读有无 seek／错误行；搜索深结果、长替换规则、自动翻页长内容 | OPEN；裁剪不得溢出或出现纯空白，深滚定位与手势接管须连续 |
| 分页基础 | 五种模式对应入口、首末页、同章／跨章、前进／后退、点击／滑动／取消、冷页／热页、空缺邻页 | 已有平移／覆盖部分日间操作，以及滑动同章两轮镜像 Hold trace；全矩阵仍 OPEN |
| 输入序列 | 中央／边缘起手、25ms 稀疏 MOVE、仅 UP 越阈值、最后 MOVE 与 UP 并发、反向、长按、文字选择、多指、系统 CANCEL、下拉书签 | 本地部分算法／探针通过或复现问题；匹配运行验证 OPEN |
| 连续操作 | next、previous、交替各至少 100 次；同页再抓至少 10 次；收尾不同阶段接管；rapid 净目标抵消与边界 | OPEN；事务、页 identity 和内容锚点都要核对，不能仅看页码 |
| 失败注入 | 纹理晚到／乱序／失败、图片迟到、慢存储、成功回执迟到、写入结果未知、回滚与退出冲突 | OPEN；验证真实状态演进与幂等，非只断言源码里有 guard |
| 生命周期 | 页面退出／后台恢复、模式切换、主题／字体／缩进变化、旋转／尺寸／density 变化、减少动态效果、surface 重建 | OPEN；旧版本不能覆盖新视口，保存不可丢失 |
| 视觉合成 | 日间／夜间、首／终帧、提交／回滚、快速翻页、章节切换、TTS／AutoPage 高亮、Alpha、根背景、颜色空间、纹理采样、书沟阴影 | 局部日间录像未捕获大空帧；全矩阵 OPEN。截图中的 alpha 通常已合成，不能证明 EGL 原始 alpha |
| 延迟 | 输入接收→Native 消费→求解→buffer submit→可验证呈现，记录 seq／时钟域／generation；热页首响应 | OPEN；输入到提交与真实触摸到显示分别报告。VM 不能证明真机触控端到端延迟 |
| 帧性能 | 原合同 60Hz 活动帧 p95≤16.7ms、p99≤20ms；求解 CPU≤.25ms；GPU p95≤4ms；热页首帧一 VSync 内 | 均是待测目标，非当前结果。记录 UI build/layout、native upload、GC、RS/GPU 与错过帧预算；固定实际刷新率 |
| 触控目标 | 原合同触点到送显 p95≤25ms、p99≤33ms；连续 100 页无>33ms 帧 | OPEN；先澄清“送显”是否只是提交。录屏 PTS 和工具命令耗时不能完成该验收；真机物理指标本轮不安排 |
| 高刷新率 | 120Hz 下约8.33ms帧周期、稀疏输入仍连续更新 | OPEN；此次手机场景只观察到60Hz，不能声称120Hz达标；VM也只能按实测模式报告 |
| 吞吐 | rapid≥4.5页／秒，准备、保存、呈现交接全部计入 | 合同待修订后验证；普通600ms单页路径不得混算 |
| 资源／功耗 | 四槽、每张3M像素RGB8逻辑36,000,000字节（约34.3MiB）之外的总峰值；长跑内存；GPU／CPU空闲、背景任务、热状态 | OPEN；VM可测泄漏趋势／调度，不能替代手机温升、电池及低端机结论 |
| Figma 一致性 | 端点与完整过程、小把手→进度→各 actor 轨迹、模糊／裁剪和非空中段 | 本轮未新拉取 Figma 或完成逐帧对照；源码／构建／安装不能证明一致 |
| 产物与测试覆盖 | 当前生产组件执行测试、GL mock、非增量构建、签名、manifest、Native库身份、保数据部署回执、生产启动 | 后续已绑定既有 manifest/部署回执并 inspect，通过部分 VM 生产路径取证；本任务未新构建/安装。旧目录测试主体仍读 FullDirectoryPanel，需补当前组件执行覆盖 |

以上所有 OPEN 项继续留在验收清单，不因用户这次没看到闪烁、某个本地测试 PASS 或 VM 完成单个场景而关闭。

**9. 当前本地验证及其限制**

- 本次控制栏 runtime、policy、presentation、playback-content、directory-model、p0、directory-content 测试通过；其中 directory-content 主体仍覆盖旧 FullDirectoryPanel，不足以证明新目录滚动形变。
- 本次 Native solver 235、motion 264、renderer GL-state 358、present barrier 131 checks 通过；ArkTS motion／architecture 检查通过。这些验证既有行为和 mock GL 状态，不证明实际显示屏或输入延迟。
- 其他模式的安全源码探针再次复现前述逻辑错误。已有标准测试 PASS 与探针揭示遗漏可以同时成立，需要补操作序列级回归。
- 本轮其他模式共 8 个 Mock 情景断言通过，含纵向无呈现、最终 UP 落后和第二指覆盖起点；这里的“断言通过”代表缺陷复现成功。Native motion 的旧断言仍要求 24ms／追赶／240ms 下限，后续须随新合同修订，不得将旧算法 PASS 当作优化通过。
- 本任务只新增证据／报告；没有把优化建议写入业务实现，没有生成候选 HAP。后续 VM 取证确认了性能缺陷，不代表修复通过。

**10. 与之前发现的项目逐项对应，防止遗漏**

| 之前的清单 | 本报告对应编号／位置 |
|---|---|
| 原始 Native 透明 clear、release、generation、tau 反向、主题底、统一状态机、阴影 alpha | F01–F06／F09，N04／N06／N08／N14／N15 |
| 固定槽位但条件分支仍重挂载，提交清邻页 | F05，P06 |
| 24ms、中央起手追赶、最后触点、240–600ms、不能再抓 | N01–N06 |
| 隔离可见上下文、三页准备窗口／四GPU槽、旧版本保留、冷手势续接 | N07／N13，P01，第7部分共同不变量 |
| 隐藏预热当前页、首帧匹配、submitted/presented语义、旧回调防护 | N04／N08，第7部分共同不变量；预热仅在真实版本匹配后启用 |
| 净目标、普通600ms与rapid吞吐冲突、上传预算、固定网格/EGL/3draw | N11／N12，P05，第8部分吞吐 |
| 光影、颜色空间、主题、动态TTS/自动翻页高亮 | N09／N10，第8部分视觉合成 |
| 书首尾反向抵消、UP误判、纵向X不跟随、最终offset、none取消 | P02–P05 |
| 滚动段内锚点、退出在途保存、保存失败、Core超时未知 | R01／R02，P07 |
| 图片重排、多指、无动画排版、上一章逐页扫描 | R03–R05，P09 |
| 位置／倾角连续、100次各方向、10次再抓、晚到/乱序/失败/旋转/退出 | 第7部分共同不变量、第8部分输入与失败矩阵 |
| 首帧、p95/p99、真实触控、120Hz、内存总峰值、功耗、空闲无轮询 | N13，第8部分性能／资源矩阵 |
| 控制栏中途停留、清晰度平台、慢尾、缓存／布局／滚动／Path／模糊 | C01–C07 |
| 新发现页码相同但正文锚点不同、动态高亮捕获、旧生产测试覆盖遗漏 | P08，N09，第9部分 |
| 后续用户补充平移方向差异、仍按着时迟起动、各种情景不跟手 | P10–P15，方向与按住停动及扩大审计；覆盖热／冷、首拖／连拖、旧队列／Auto Page／音量键、回撤／跨原点、松手／再抓和异常时间戳 |

新增扩大审计探针有11条情景／复杂度输出，包含旧请求在新手指仍按着时双向启动动画和保存。编译参数表达式已和当前源码逐项核对；后续布局、UI 更新、GC 和重挂载已有同包 VM 数据，提交/实际显示关联仍缺。当前 Harmony 与 Core 均 dirty；后续取证已经绑定被测 manifest 和关键源码指纹，不代表全工作区或释放后的安装状态。详细证据与顺序见扩大审计文件。

下一步的具体执行入口是 A 阶段的生产路径取证与合同口径对齐，并推进 B 阶段确定性修复和 C 阶段控制栏优化。设备侧只使用已经存在、身份确认、启动完成且 SceneBoard 稳定的 VM，先取得共享锁；保留用户数据，按唯一 HAP 流水线部署。遇到 VM 系统故障保留现场，不通过新建／重置／清数据绕过。真机性能、温升和物理触控验收继续保留，不会自动重新占用真机。
