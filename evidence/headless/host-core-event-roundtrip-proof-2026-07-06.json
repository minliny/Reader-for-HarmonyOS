{
  "schemaVersion": "headless-test.v1",
  "proofName": "HostCoreEventRoundTripProof (Core event round-trip via NAPI)",
  "tier": "CORE EVENT ROUND-TRIP (DevEco headless on physical device)",
  "device": "7GL0226317009033",
  "bundleName": "reader.minliny.testpackage",
  "abilityName": "EntryAbility",
  "trigger": "hdc shell aa start -b reader.minliny.testpackage -a EntryAbility --ps readerHeadlessTest 1",
  "ranAt": "2026-07-06T12:00:34.156Z",
  "status": "PASS",
  "proofSuite": {
    "suite": "Host Core Event Round-Trip Proof (NAPI readEvent → parseEvent → handleHostRequest → complete/fail → result)",
    "passed": 6,
    "failed": 0,
    "total": 6
  },
  "fullRunSummary": {
    "totalPassed": 832,
    "totalFailed": 4,
    "totalTests": 836,
    "note": "4 failures are pre-existing (Import 1 + Core Adapter Complete 1 + Release Gate 2), unrelated to this proof"
  },
  "verificationScope": [
    "init: ReaderCoreClient initialized (NAPI bridge loaded) — libreader_core_napi.so",
    "ping: runtime.ping command → result event round-trip completes (non-timeout) — basic command path without host.request",
    "ping: result data is non-null",
    "book.search: command → host.request(http.execute) → handleHostRequest → complete/fail → result (full round-trip closed, non-timeout) — Core emits real host.request through NAPI",
    "book.search: error mentions http/host (Core reached host.request stage before failing) — .invalid TLD ensures DNS failure, verifying failHostRequest path",
    "control: book.search with searchResponse completes without host.request (non-timeout) — distinguishes with-host.request vs without-host.request paths"
  ],
  "roundTripPath": [
    "Host: ReaderCoreClient.sendCommand(method, params)",
    "Core: process command, emit host.request (http.execute for book.search)",
    "Host: ReaderCoreClient.awaitResult → runtime.readEvent(0) → parseEvent(raw)",
    "Host: parseEvent identifies type='host.request' → handleHostRequest(parsed)",
    "Host: handleHostRequest → adapter.dispatch(capability, params) → http.execute handler (OHOSHTTPHostTransport)",
    "Host: http.execute fails (.invalid TLD DNS resolution) → reply.ok=false",
    "Host: handleHostRequest → runtime.failHostRequest(operationId, errorObj)",
    "Core: receive host.error, continue processing, emit result/error event",
    "Host: awaitResult → readEvent → parseEvent identifies type='result'/'error' → return/throw"
  ],
  "keyDistinction": {
    "previous": "HostRouterDispatchProof (33P/33 PASS) calls adapter.dispatch directly with synthetic params — bypasses NAPI readEvent/parseEvent/handleHostRequest",
    "current": "HostCoreEventRoundTripProof (6P/6 PASS) issues real Core commands via sendCommand + awaitResult — Core drives host.request emission through NAPI, HostClient production polling path routes it",
    " significance": "HarmonyOS router layer is NOT a synthetic-params proof — Core actually emits host.request events that traverse the full NAPI round-trip"
  },
  "canEnterMainline": "still false (memoryOwned/threadingSafe/ciGreen hardcoded false — this proof verifies Core event round-trip path only, real executors remain stubs)",
  "notClaimed": [
    "real ArkWeb execution (stub returns NOT_IMPLEMENTED for webview.evaluateJavaScript)",
    "real @ohos.net.http success (http.execute uses real OHOSHTTPHostTransport but targets .invalid TLD to avoid real network)",
    "real media download / anti-bot challenge (stubs return NOT_IMPLEMENTED)",
    "App-level UI reading chain (Phase 5)"
  ]
}
