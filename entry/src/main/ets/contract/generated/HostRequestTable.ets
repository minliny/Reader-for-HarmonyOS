// GENERATED by scripts/gen_contracts.mjs — DO NOT EDIT BY HAND.
// Source: Reader UI/contracts/fixtures/*.json (read-only contract).
// Re-run `npm run gen:contracts` after any contract change.

export type HostRequestType = 'http.execute' | 'http.cancel' | 'webview.open' | 'webview.close' | 'webview.evaluate' | 'cookie.get' | 'cookie.set' | 'cookie.clear' | 'file.read' | 'file.write' | 'file.delete' | 'storage.path' | 'persistence.get' | 'persistence.put' | 'credential.get' | 'credential.set' | 'credential.delete' | 'tts.system.start' | 'tts.system.stop' | 'tts.system.pause' | 'tts.system.resume' | 'permission.request' | 'permission.check' | 'background.schedule' | 'background.cancel' | 'timer.foreground.arm' | 'timer.foreground.cancel' | 'notification.show' | 'notification.cancel' | 'share.invoke' | 'clipboard.copy' | 'clipboard.paste' | 'device.vibrate' | 'device.screen.keep-on' | 'device.screen.release' | 'file.select' | 'font.registerFile' | 'font.unregisterFile' | 'clipboard.read' | 'clipboard.write' | 'tts.start' | 'tts.stop' | 'tts.pause' | 'brightness.set' | 'brightness.get' | 'screen.keepAwake' | 'screen.allowSleep' | 'haptics.light' | 'haptics.medium' | 'haptics.heavy' | 'network.status' | 'webdav.connect' | 'webdav.backup' | 'webdav.restore' | 'share.text' | 'share.file' | 'background.task.start' | 'background.task.end';
export type HostRequestInitiator = 'core' | 'reducer';

export class HostRequestTable {
  static readonly SCHEMA_VERSION: string = '1.2.0';
  static readonly ALL: HostRequestType[] = [
    'http.execute',
    'http.cancel',
    'webview.open',
    'webview.close',
    'webview.evaluate',
    'cookie.get',
    'cookie.set',
    'cookie.clear',
    'file.read',
    'file.write',
    'file.delete',
    'storage.path',
    'persistence.get',
    'persistence.put',
    'credential.get',
    'credential.set',
    'credential.delete',
    'tts.system.start',
    'tts.system.stop',
    'tts.system.pause',
    'tts.system.resume',
    'permission.request',
    'permission.check',
    'background.schedule',
    'background.cancel',
    'timer.foreground.arm',
    'timer.foreground.cancel',
    'notification.show',
    'notification.cancel',
    'share.invoke',
    'clipboard.copy',
    'clipboard.paste',
    'device.vibrate',
    'device.screen.keep-on',
    'device.screen.release',
    'file.select',
    'font.registerFile',
    'font.unregisterFile',
    'clipboard.read',
    'clipboard.write',
    'tts.start',
    'tts.stop',
    'tts.pause',
    'brightness.set',
    'brightness.get',
    'screen.keepAwake',
    'screen.allowSleep',
    'haptics.light',
    'haptics.medium',
    'haptics.heavy',
    'network.status',
    'webdav.connect',
    'webdav.backup',
    'webdav.restore',
    'share.text',
    'share.file',
    'background.task.start',
    'background.task.end'
  ];

  static has(type: string): boolean {
    return HostRequestTable.ALL.indexOf(type as HostRequestType) >= 0;
  }
}
