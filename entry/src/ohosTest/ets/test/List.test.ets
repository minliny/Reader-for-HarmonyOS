// Device-test entrypoint. The test bodies remain in entry/src/test so there
// is exactly one suite definition for both compile-time and on-device gates.
import testsuite from '../../../test/List.test';

export default testsuite;
